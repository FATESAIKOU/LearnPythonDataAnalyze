# Section2: DataFrame（2次元データ）- 応用問題

## 🚀 この応用問題について

演習で学んだ基礎を使って、より実践的な問題に挑戦しましょう。
分からない場合は、技術説明を見直したり、AI エージェントに質問してください。

---

## 応用問題1: 社員データの作成と確認

### 問題

以下の社員データを DataFrame として作成し、基本情報を確認してください。

| 社員ID | 名前 | 部署 | 年齢 | 給料 |
|--------|------|------|------|------|
| E001 | 田中 | 営業 | 28 | 350 |
| E002 | 鈴木 | 開発 | 32 | 450 |
| E003 | 佐藤 | 総務 | 25 | 300 |
| E004 | 高橋 | 開発 | 35 | 520 |
| E005 | 伊藤 | 営業 | 29 | 380 |

1. 社員IDをインデックスとして DataFrame を作成
2. shape, columns, index を確認
3. 統計情報を表示

### 期待される出力

```
社員データ:
      名前  部署  年齢   給料
E001  田中  営業   28  350
E002  鈴木  開発   32  450
E003  佐藤  総務   25  300
E004  高橋  開発   35  520
E005  伊藤  営業   29  380

shape: (5, 4)
columns: ['名前', '部署', '年齢', '給料']
index: ['E001', 'E002', 'E003', 'E004', 'E005']

統計情報:
             年齢          給料
count   5.000000    5.000000
mean   29.800000  400.000000
...
```

<details>
<summary>解答例を見る</summary>

**基本解法: 辞書から作成**

```python
import pandas as pd

data = {
    "名前": ["田中", "鈴木", "佐藤", "高橋", "伊藤"],
    "部署": ["営業", "開発", "総務", "開発", "営業"],
    "年齢": [28, 32, 25, 35, 29],
    "給料": [350, 450, 300, 520, 380]
}
index = ["E001", "E002", "E003", "E004", "E005"]

df = pd.DataFrame(data, index=index)

print("社員データ:")
print(df)

print("\nshape:", df.shape)
print("columns:", df.columns.tolist())
print("index:", df.index.tolist())

print("\n統計情報:")
print(df.describe())
```

**発展解法: リストのリストから作成**

```python
import pandas as pd

data = [
    ["田中", "営業", 28, 350],
    ["鈴木", "開発", 32, 450],
    ["佐藤", "総務", 25, 300],
    ["高橋", "開発", 35, 520],
    ["伊藤", "営業", 29, 380]
]
columns = ["名前", "部署", "年齢", "給料"]
index = ["E001", "E002", "E003", "E004", "E005"]

df = pd.DataFrame(data, columns=columns, index=index)

print("社員データ:")
print(df)

print("\nshape:", df.shape)
print("columns:", df.columns.tolist())
print("index:", df.index.tolist())

print("\n統計情報:")
print(df.describe())
```

</details>

---

## 応用問題2: テスト結果の作成と分析

### 問題

5人の生徒の3科目のテスト結果を DataFrame として作成し、分析してください。

```python
import pandas as pd
import numpy as np

np.random.seed(42)
students = ["田中", "鈴木", "佐藤", "高橋", "伊藤"]
```

1. 国語、数学、英語の点数をランダムに生成（50〜100点）
2. DataFrame を作成（生徒名をインデックスに）
3. 各科目の平均点を確認

### 期待される出力

```
テスト結果:
    国語  数学  英語
田中   81   95   58
鈴木   70   80   58
...

各科目の平均点:
国語    XX.X
数学    XX.X
英語    XX.X
dtype: float64
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで平均計算**

```python
import pandas as pd
import numpy as np

np.random.seed(42)
students = ["田中", "鈴木", "佐藤", "高橋", "伊藤"]

# ランダムに点数を生成
scores = np.random.randint(50, 101, size=(5, 3))
df = pd.DataFrame(scores, columns=["国語", "数学", "英語"], index=students)

print("テスト結果:")
print(df)

# 各科目の平均を手計算
print("\n各科目の平均点:")
for col in df.columns:
    total = 0
    for i in range(len(df)):
        total = total + df[col].iloc[i]
    mean = total / len(df)
    print(f"{col}: {round(mean, 1)}")
```

**発展解法: mean() を使用**

```python
import pandas as pd
import numpy as np

np.random.seed(42)
students = ["田中", "鈴木", "佐藤", "高橋", "伊藤"]

scores = np.random.randint(50, 101, size=(5, 3))
df = pd.DataFrame(scores, columns=["国語", "数学", "英語"], index=students)

print("テスト結果:")
print(df)

print("\n各科目の平均点:")
print(df.mean())
```

</details>

---

## 応用問題3: 売上データの確認

### 問題

月別売上データを作成し、head() と tail() で確認してください。

```python
import pandas as pd
import numpy as np

np.random.seed(123)
months = pd.date_range("2024-01", periods=12, freq="ME").strftime("%Y-%m").tolist()
```

1. 商品A, 商品B, 商品C の12ヶ月分の売上を生成（100〜500）
2. DataFrame を作成（月をインデックスに）
3. 先頭3ヶ月と末尾3ヶ月を表示

### 期待される出力

```
先頭3ヶ月:
         商品A  商品B  商品C
2024-01   XXX   XXX   XXX
2024-02   XXX   XXX   XXX
2024-03   XXX   XXX   XXX

末尾3ヶ月:
         商品A  商品B  商品C
2024-10   XXX   XXX   XXX
2024-11   XXX   XXX   XXX
2024-12   XXX   XXX   XXX
```

<details>
<summary>解答例を見る</summary>

**基本解法**

```python
import pandas as pd
import numpy as np

np.random.seed(123)
months = pd.date_range("2024-01", periods=12, freq="ME").strftime("%Y-%m").tolist()

# 売上データを生成
sales_a = np.random.randint(100, 501, size=12)
sales_b = np.random.randint(100, 501, size=12)
sales_c = np.random.randint(100, 501, size=12)

data = {
    "商品A": sales_a,
    "商品B": sales_b,
    "商品C": sales_c
}
df = pd.DataFrame(data, index=months)

print("全データ:")
print(df)

print("\n先頭3ヶ月:")
print(df.head(3))

print("\n末尾3ヶ月:")
print(df.tail(3))
```

**発展解法: NumPy の2次元配列で一括生成**

```python
import pandas as pd
import numpy as np

np.random.seed(123)
months = pd.date_range("2024-01", periods=12, freq="ME").strftime("%Y-%m").tolist()

sales = np.random.randint(100, 501, size=(12, 3))
df = pd.DataFrame(sales, columns=["商品A", "商品B", "商品C"], index=months)

print("先頭3ヶ月:")
print(df.head(3))

print("\n末尾3ヶ月:")
print(df.tail(3))
```

</details>

---

## 応用問題4: 欠損値のあるデータの確認

### 問題

欠損値を含むデータを作成し、info() で確認してください。

```python
import pandas as pd
import numpy as np

data = {
    "名前": ["田中", "鈴木", "佐藤", "高橋", "伊藤"],
    "年齢": [25, np.nan, 28, 35, 22],           # 1つ欠損
    "部署": ["営業", "開発", None, "開発", "営業"],  # 1つ欠損
    "給料": [300, 450, 380, np.nan, np.nan]     # 2つ欠損
}
```

1. DataFrame を作成
2. info() で欠損値の状況を確認
3. 各列の欠損していない値の数を確認

### 期待される出力

```
DataFrame:
   名前    年齢   部署     給料
0  田中  25.0   営業  300.0
1  鈴木   NaN   開発  450.0
2  佐藤  28.0  None  380.0
3  高橋  35.0   開発    NaN
4  伊藤  22.0   営業    NaN

info():
...
 0   名前    5 non-null      object
 1   年齢    4 non-null      float64  ← 1つ欠損
 2   部署    4 non-null      object   ← 1つ欠損
 3   給料    3 non-null      float64  ← 2つ欠損
...
```

<details>
<summary>解答例を見る</summary>

**基本解法**

```python
import pandas as pd
import numpy as np

data = {
    "名前": ["田中", "鈴木", "佐藤", "高橋", "伊藤"],
    "年齢": [25, np.nan, 28, 35, 22],
    "部署": ["営業", "開発", None, "開発", "営業"],
    "給料": [300, 450, 380, np.nan, np.nan]
}
df = pd.DataFrame(data)

print("DataFrame:")
print(df)

print("\ninfo():")
df.info()

# 欠損値の数を手計算
print("\n各列の欠損していない値の数:")
for col in df.columns:
    count = 0
    for val in df[col]:
        if pd.notna(val):
            count = count + 1
    print(f"{col}: {count}")
```

**発展解法: count() メソッドを使用**

```python
import pandas as pd
import numpy as np

data = {
    "名前": ["田中", "鈴木", "佐藤", "高橋", "伊藤"],
    "年齢": [25, np.nan, 28, 35, 22],
    "部署": ["営業", "開発", None, "開発", "営業"],
    "給料": [300, 450, 380, np.nan, np.nan]
}
df = pd.DataFrame(data)

print("DataFrame:")
print(df)

print("\ninfo():")
df.info()

print("\n各列の欠損していない値の数:")
print(df.count())
```

</details>

---

## 応用問題5: 複数のデータソースから DataFrame を作成

### 問題

異なる形式のデータを組み合わせて DataFrame を作成してください。

```python
import pandas as pd

# 既存のデータ
names = ["田中", "鈴木", "佐藤"]
ages = [25, 30, 28]

# 追加する辞書データ
extra_data = {"給料": [300, 450, 380], "部署": ["営業", "開発", "総務"]}
```

1. names と ages を辞書にまとめる
2. extra_data を追加
3. 1つの DataFrame として作成

### 期待される出力

```
完成した DataFrame:
   名前  年齢   給料  部署
0  田中   25  300  営業
1  鈴木   30  450  開発
2  佐藤   28  380  総務
```

<details>
<summary>解答例を見る</summary>

**基本解法: 辞書を手動でマージ**

```python
import pandas as pd

names = ["田中", "鈴木", "佐藤"]
ages = [25, 30, 28]
extra_data = {"給料": [300, 450, 380], "部署": ["営業", "開発", "総務"]}

# 辞書を作成
data = {
    "名前": names,
    "年齢": ages
}

# extra_data を追加
for key in extra_data:
    data[key] = extra_data[key]

df = pd.DataFrame(data)
print("完成した DataFrame:")
print(df)
```

**発展解法: 辞書のアンパック**

```python
import pandas as pd

names = ["田中", "鈴木", "佐藤"]
ages = [25, 30, 28]
extra_data = {"給料": [300, 450, 380], "部署": ["営業", "開発", "総務"]}

# 辞書を統合
data = {"名前": names, "年齢": ages, **extra_data}

df = pd.DataFrame(data)
print("完成した DataFrame:")
print(df)
```

</details>

---

## 🏆 チャレンジ問題: DataFrame の比較分析

### 問題

2つの期間のデータを DataFrame で作成し、比較分析してください。

```python
import pandas as pd
import numpy as np

np.random.seed(42)
products = ["商品A", "商品B", "商品C", "商品D", "商品E"]
```

1. 上半期（4〜9月）の売上データを生成（100〜500）
2. 下半期（10〜3月）の売上データを生成（100〜500）
3. 両方の describe() を表示して比較
4. どちらの期間が売上が良かったか判定

### 期待される出力

```
上半期の統計:
          商品A   商品B   商品C   商品D   商品E
count   6.0    6.0    6.0    6.0    6.0
mean  XXX.X  XXX.X  XXX.X  XXX.X  XXX.X
...

下半期の統計:
          商品A   商品B   商品C   商品D   商品E
count   6.0    6.0    6.0    6.0    6.0
mean  XXX.X  XXX.X  XXX.X  XXX.X  XXX.X
...

上半期の合計: XXXX
下半期の合計: XXXX
売上が良かったのは: XXX期
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで合計計算**

```python
import pandas as pd
import numpy as np

np.random.seed(42)
products = ["商品A", "商品B", "商品C", "商品D", "商品E"]
months_h1 = ["4月", "5月", "6月", "7月", "8月", "9月"]
months_h2 = ["10月", "11月", "12月", "1月", "2月", "3月"]

# 上半期データ
sales_h1 = np.random.randint(100, 501, size=(6, 5))
df_h1 = pd.DataFrame(sales_h1, columns=products, index=months_h1)

# 下半期データ
sales_h2 = np.random.randint(100, 501, size=(6, 5))
df_h2 = pd.DataFrame(sales_h2, columns=products, index=months_h2)

print("上半期データ:")
print(df_h1)
print("\n上半期の統計:")
print(df_h1.describe())

print("\n下半期データ:")
print(df_h2)
print("\n下半期の統計:")
print(df_h2.describe())

# 合計を計算（手動）
total_h1 = 0
for col in df_h1.columns:
    for val in df_h1[col]:
        total_h1 = total_h1 + val

total_h2 = 0
for col in df_h2.columns:
    for val in df_h2[col]:
        total_h2 = total_h2 + val

print(f"\n上半期の合計: {total_h1}")
print(f"下半期の合計: {total_h2}")

if total_h1 > total_h2:
    print("売上が良かったのは: 上半期")
else:
    print("売上が良かったのは: 下半期")
```

**発展解法: sum() を使用**

```python
import pandas as pd
import numpy as np

np.random.seed(42)
products = ["商品A", "商品B", "商品C", "商品D", "商品E"]
months_h1 = ["4月", "5月", "6月", "7月", "8月", "9月"]
months_h2 = ["10月", "11月", "12月", "1月", "2月", "3月"]

sales_h1 = np.random.randint(100, 501, size=(6, 5))
df_h1 = pd.DataFrame(sales_h1, columns=products, index=months_h1)

sales_h2 = np.random.randint(100, 501, size=(6, 5))
df_h2 = pd.DataFrame(sales_h2, columns=products, index=months_h2)

print("上半期の統計:")
print(df_h1.describe())

print("\n下半期の統計:")
print(df_h2.describe())

total_h1 = df_h1.values.sum()
total_h2 = df_h2.values.sum()

print(f"\n上半期の合計: {total_h1}")
print(f"下半期の合計: {total_h2}")
print(f"売上が良かったのは: {'上半期' if total_h1 > total_h2 else '下半期'}")
```

</details>

---

おめでとうございます！🎉 Section2 を完了しました！
次は Section3: データの読み書きに進みましょう！
