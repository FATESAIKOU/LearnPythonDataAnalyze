# Section1: Series（1次元データ）

## 🎯 このセクションの目標

Pandas の1次元データ構造「Series」を理解し、基本的な操作ができるようになる。

---

## Pandas とは

Pandas は、Python でデータ分析を行うためのライブラリです。
Excel のようなテーブル操作をプログラムで実現できます。

```
┌─────────────────────────────────────────────────────────────┐
│                    データ分析ライブラリの関係               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    ┌──────────┐     ┌──────────┐     ┌──────────────┐       │
│    │  NumPy   │ ──► │  Pandas  │ ──► │ Matplotlib   │       │
│    │ 数値計算 │     │データ操作│     │    可視化    │       │
│    └──────────┘     └──────────┘     └──────────────┘       │
│         │                │                   │              │
│         ▼                ▼                   ▼              │
│    ┌──────────────────────────────────────────────┐         │
│    │           データ分析・機械学習               │         │
│    └──────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Pandas の読み込み

```python
import pandas as pd   # pd は慣例的な略称
```

---

## Series の全体像

Series は「インデックス付きの1次元配列」です。

```
┌─────────────────────────────────────────────────────────────┐
│                    Series の構造                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│          ┌──────────────────────────────────┐               │
│          │          Series                  │               │
│          ├────────────┬─────────────────────┤               │
│          │  index     │      values         │               │
│          │ (ラベル)   │     (データ)        │               │
│          ├────────────┼─────────────────────┤               │
│          │    0       │       100           │               │
│          │    1       │       200           │               │
│          │    2       │       300           │               │
│          │    3       │       400           │               │
│          └────────────┴─────────────────────┘               │
│               ↑                  ↑                          │
│            自動で           リストから                      │
│           0,1,2...         作成したデータ                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

        NumPy配列との違い:
        ┌────────────────┐      ┌─────────────────────────┐
        │  NumPy array   │      │    Pandas Series        │
        ├────────────────┤      ├────────────┬────────────┤
        │      100       │      │  index     │   value    │
        │      200       │  vs  │    0       │    100     │
        │      300       │      │    1       │    200     │
        │      400       │      │    2       │    300     │
        └────────────────┘      │    3       │    400     │
        (位置だけで管理)        └────────────┴────────────┘
                                (ラベルでも管理できる)
```

---

## Series の作成

### リストから作成

```
┌─────────────────────────────────────────────────────────────┐
│                リストから Series を作成                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    Python リスト                      Pandas Series         │
│    ┌────────────┐                   ┌──────┬───────┐        │
│    │    100     │                   │   0  │  100  │        │
│    │    200     │   pd.Series() ►   │   1  │  200  │        │
│    │    300     │   ─────────────   │   2  │  300  │        │
│    │    400     │                   │   3  │  400  │        │
│    └────────────┘                   └──────┴───────┘        │
│                                      index  values          │
│                                     (自動生成)              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

# リストから Series を作成
data = [100, 200, 300, 400]
s = pd.Series(data)
print(s)
```

出力:
```
0    100
1    200
2    300
3    400
dtype: int64
```

### 辞書から作成

```
┌─────────────────────────────────────────────────────────────┐
│                辞書から Series を作成                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    Python 辞書                        Pandas Series         │
│    ┌──────────────────┐             ┌────────┬───────┐      │
│    │ "apple"  :  150  │             │ apple  │  150  │      │
│    │ "banana" :  100  │  pd.Series► │ banana │  100  │      │
│    │ "orange" :  200  │ ──────────► │ orange │  200  │      │
│    └──────────────────┘             └────────┴───────┘      │
│      キー     値                      index    values       │
│       │       │                        │         │          │
│       └───────┼────────────────────────┘         │          │
│               └──────────────────────────────────┘          │
│           辞書のキーがインデックスになる                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
# 辞書から Series を作成
prices = {"apple": 150, "banana": 100, "orange": 200}
s = pd.Series(prices)
print(s)
```

出力:
```
apple     150
banana    100
orange    200
dtype: int64
```

### インデックスを指定して作成

```
┌─────────────────────────────────────────────────────────────┐
│              インデックスを明示的に指定                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    data = [85, 90, 78, 92]                                  │
│    index = ["国語", "数学", "英語", "理科"]                 │
│                                                             │
│          ┌──────────┐          ┌────────┬───────┐           │
│    data: │ 85,90... │   ─►     │  国語  │   85  │           │
│          └──────────┘          │  数学  │   90  │           │
│                     結合       │  英語  │   78  │           │
│          ┌──────────┐   ─►     │  理科  │   92  │           │
│   index: │ 国語,... │          └────────┴───────┘           │
│          └──────────┘          カスタムインデックス         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
# インデックスを指定して作成
scores = pd.Series([85, 90, 78, 92], 
                   index=["国語", "数学", "英語", "理科"])
print(scores)
```

出力:
```
国語    85
数学    90
英語    78
理科    92
dtype: int64
```

---

## Series の属性

Series には様々な情報を取得する属性があります。

```
┌───────────────────────────────────────────────────────────────┐
│                  Series の主要な属性                          │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│         s = pd.Series([85, 90, 78, 92],                       │
│                       index=["国語", "数学", "英語", "理科"]) │
│                                                               │
│         ┌────────┬───────┐                                    │
│         │  国語  │   85  │                                    │
│         │  数学  │   90  │                                    │
│         │  英語  │   78  │                                    │
│         │  理科  │   92  │                                    │
│         └────────┴───────┘                                    │
│             │        │                                        │
│    ┌────────┼────────┼───────────────────────────────┐        │
│    │        │        │                               │        │
│    ▼        │        ▼                               ▼        │
│ s.index     │     s.values                        s.dtype     │
│ ["国語",    │     [85, 90, 78, 92]                 int64      │
│  "数学",    │      (NumPy配列)                    (型情報)    │
│  "英語",    │                                                 │
│  "理科"]    │                                                 │
│             ▼                                                 │
│          s.size = 4 (要素数)                                  │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

s = pd.Series([85, 90, 78, 92], 
              index=["国語", "数学", "英語", "理科"])

print("index:", s.index)    # インデックス
print("values:", s.values)  # 値（NumPy配列）
print("dtype:", s.dtype)    # データ型
print("size:", s.size)      # 要素数
```

出力:
```
index: Index(['国語', '数学', '英語', '理科'], dtype='object')
values: [85 90 78 92]
dtype: int64
size: 4
```

---

## Series の要素アクセス

Series の要素には、位置（番号）またはラベル（インデックス）でアクセスできます。

```
┌─────────────────────────────────────────────────────────────┐
│                  要素アクセスの方法                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│         ┌────────┬───────┐                                  │
│         │  国語  │   85  │ ◄── s["国語"] または s[0]        │
│         │  数学  │   90  │ ◄── s["数学"] または s[1]        │
│         │  英語  │   78  │ ◄── s["英語"] または s[2]        │
│         │  理科  │   92  │ ◄── s["理科"] または s[3]        │
│         └────────┴───────┘                                  │
│                                                             │
│    ┌────────────────────────────────────────────────────┐   │
│    │  ラベルでアクセス           位置でアクセス         │   │
│    │  s["数学"] → 90            s[1] → 90               │   │
│    │  s["理科"] → 92            s[3] → 92               │   │
│    │  s["国語"] → 85            s[0] → 85               │   │
│    └────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

s = pd.Series([85, 90, 78, 92], 
              index=["国語", "数学", "英語", "理科"])

# ラベルでアクセス
print("数学の点数:", s["数学"])

# 位置でアクセス
print("2番目の点数:", s[1])

# 複数要素の取得
print("国語と英語:")
print(s[["国語", "英語"]])
```

出力:
```
数学の点数: 90
2番目の点数: 90
国語と英語:
国語    85
英語    78
dtype: int64
```

---

## Series のスライス

連続した複数の要素を取得できます。

```
┌─────────────────────────────────────────────────────────────┐
│                  スライスの動作                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│         ┌────────┬───────┐                                  │
│    位置 │ index  │ value │                                  │
│    [0]  │  国語  │   85  │ ─┐                               │
│    [1]  │  数学  │   90  │  ├── s[0:3] → 国語, 数学, 英語   │
│    [2]  │  英語  │   78  │ ─┘  (位置スライス: 終了含まず)   │
│    [3]  │  理科  │   92  │                                  │
│         └────────┴───────┘                                  │
│                                                             │
│    【位置スライス s[0:3]】    【ラベルスライス】            │
│    ┌────────┬───────┐         s["国語":"英語"]              │
│    │  国語  │   85  │         ┌────────┬───────┐            │
│    │  数学  │   90  │         │  国語  │   85  │            │
│    │  英語  │   78  │         │  数学  │   90  │            │
│    └────────┴───────┘         │  英語  │   78  │            │
│    終了位置[3]は含まない      └────────┴───────┘            │
│                                終了ラベルも含む！           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

s = pd.Series([85, 90, 78, 92], 
              index=["国語", "数学", "英語", "理科"])

# 位置でスライス（終了位置は含まない）
print("位置スライス s[0:3]:")
print(s[0:3])

print()

# ラベルでスライス（終了ラベルも含む）
print("ラベルスライス s['国語':'英語']:")
print(s["国語":"英語"])
```

出力:
```
位置スライス s[0:3]:
国語    85
数学    90
英語    78
dtype: int64

ラベルスライス s['国語':'英語']:
国語    85
数学    90
英語    78
dtype: int64
```

⚠️ **注意**: ラベルでスライスする場合は終了ラベルも含まれます！

---

## Series の算術演算

Series は NumPy 配列と同様に、要素ごとの演算ができます。

```
┌─────────────────────────────────────────────────────────────┐
│                スカラー値との演算                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│      s                  s * 2                               │
│    ┌────────┬───────┐       ┌────────┬───────┐              │
│    │  国語  │   85  │       │  国語  │  170  │              │
│    │  数学  │   90  │ × 2 = │  数学  │  180  │              │
│    │  英語  │   78  │       │  英語  │  156  │              │
│    │  理科  │   92  │       │  理科  │  184  │              │
│    └────────┴───────┘       └────────┴───────┘              │
│                                                             │
│         全ての要素に2が掛けられる（ブロードキャスト）       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

s = pd.Series([85, 90, 78, 92], 
              index=["国語", "数学", "英語", "理科"])

print("元のデータ:")
print(s)

print("\n2倍:")
print(s * 2)

print("\n10点加算:")
print(s + 10)
```

### Series 同士の演算

```
┌─────────────────────────────────────────────────────────────┐
│               Series 同士の演算（インデックス一致）         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│      1学期            2学期             合計                │
│    ┌────────┬────┐   ┌────────┬────┐   ┌────────┬───────┐   │
│    │  国語  │ 85 │ + │  国語  │ 90 │ = │  国語  │  175  │   │
│    │  数学  │ 90 │   │  数学  │ 88 │   │  数学  │  178  │   │
│    │  英語  │ 78 │   │  英語  │ 82 │   │  英語  │  160  │   │
│    │  理科  │ 92 │   │  理科  │ 95 │   │  理科  │  187  │   │
│    └────────┴────┘   └────────┴────┘   └────────┴───────┘   │
│                                                             │
│     同じインデックス同士が自動的に対応して計算される        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```python
import pandas as pd

# 1学期と2学期の点数
term1 = pd.Series([85, 90, 78, 92], 
                  index=["国語", "数学", "英語", "理科"])
term2 = pd.Series([90, 88, 82, 95], 
                  index=["国語", "数学", "英語", "理科"])

print("1学期:")
print(term1)

print("\n2学期:")
print(term2)

print("\n合計:")
print(term1 + term2)

print("\n平均:")
print((term1 + term2) / 2)
```

---

## まとめ

```
┌─────────────────────────────────────────────────────────────┐
│                    Series まとめ                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  【Series の特徴】                                          │
│    ・インデックス（ラベル）付きの1次元配列                  │
│    ・辞書のようにラベルでアクセス可能                       │
│    ・NumPy配列のように演算可能                              │
│                                                             │
│  【主なメソッド・属性】                                     │
│    ┌────────────────┬────────────────────────┐              │
│    │  pd.Series()   │  Series を作成         │              │
│    │  s.index       │  インデックスを取得    │              │
│    │  s.values      │  値（配列）を取得      │              │
│    │  s.dtype       │  データ型を取得        │              │
│    │  s.size        │  要素数を取得          │              │
│    │  s["ラベル"]   │  ラベルで要素取得      │              │
│    │  s[位置]       │  位置で要素取得        │              │
│    └────────────────┴────────────────────────┘              │
│                                                             │
│  【次のステップ】                                           │
│    Section2 では Series を2次元に拡張した                   │
│    DataFrame を学びます                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```
