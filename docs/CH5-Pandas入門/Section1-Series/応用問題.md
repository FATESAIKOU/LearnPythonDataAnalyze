# Section1: Series（1次元データ）- 応用問題

## 🚀 この応用問題について

演習で学んだ基礎を使って、より実践的な問題に挑戦しましょう。
分からない場合は、技術説明を見直したり、AI エージェントに質問してください。

---

## 応用問題1: 気温データの分析

### 問題

1週間の気温データを Series として作成し、分析してください。

```python
import pandas as pd

temperatures = [22, 25, 28, 30, 27, 24, 23]
days = ["月", "火", "水", "木", "金", "土", "日"]
```

1. Series を作成
2. 金曜日の気温を取得
3. 最高気温を取得
4. 平均気温を計算

### 期待される出力

```
1週間の気温:
月    22
火    25
水    28
木    30
金    27
土    24
日    23
dtype: int64

金曜日の気温: 27
最高気温: 30
平均気温: 25.57
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループと四則演算**

```python
import pandas as pd

temperatures = [22, 25, 28, 30, 27, 24, 23]
days = ["月", "火", "水", "木", "金", "土", "日"]

# 1. Series を作成
s = pd.Series(temperatures, index=days)
print("1週間の気温:")
print(s)

# 2. 金曜日の気温
print("\n金曜日の気温:", s["金"])

# 3. 最高気温を手計算
max_temp = s[0]
for t in s:
    if t > max_temp:
        max_temp = t
print("最高気温:", max_temp)

# 4. 平均気温を手計算
total = 0
for t in s:
    total = total + t
mean_temp = total / len(s)
print("平均気温:", round(mean_temp, 2))
```

**発展解法: Series のメソッドを使用**

```python
import pandas as pd

temperatures = [22, 25, 28, 30, 27, 24, 23]
days = ["月", "火", "水", "木", "金", "土", "日"]

s = pd.Series(temperatures, index=days)
print("1週間の気温:")
print(s)

print("\n金曜日の気温:", s["金"])
print("最高気温:", s.max())
print("平均気温:", round(s.mean(), 2))
```

</details>

---

## 応用問題2: 商品価格の税込計算

### 問題

商品価格を税込価格（10%）に変換してください。

```python
import pandas as pd

prices = {"りんご": 150, "バナナ": 100, "みかん": 80, "ぶどう": 300}
```

1. 辞書から Series を作成
2. 税込価格（価格 × 1.1）を計算
3. 結果を整数に丸める

### 期待される出力

```
税抜価格:
りんご     150
バナナ     100
みかん      80
ぶどう     300
dtype: int64

税込価格:
りんご     165
バナナ     110
みかん      88
ぶどう     330
dtype: int64
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで計算**

```python
import pandas as pd

prices = {"りんご": 150, "バナナ": 100, "みかん": 80, "ぶどう": 300}
s = pd.Series(prices)
print("税抜価格:")
print(s)

# 税込価格を計算（手動で各要素を処理）
tax_included = {}
for item in s.index:
    tax_included[item] = int(s[item] * 1.1)

result = pd.Series(tax_included)
print("\n税込価格:")
print(result)
```

**発展解法: ブロードキャスト演算**

```python
import pandas as pd

prices = {"りんご": 150, "バナナ": 100, "みかん": 80, "ぶどう": 300}
s = pd.Series(prices)
print("税抜価格:")
print(s)

tax_included = (s * 1.1).astype(int)
print("\n税込価格:")
print(tax_included)
```

</details>

---

## 応用問題3: テスト成績の評価

### 問題

テストの点数から評価を判定してください（60点以上が合格）。

```python
import pandas as pd

scores = pd.Series([85, 42, 78, 55, 90, 63],
                   index=["田中", "鈴木", "佐藤", "高橋", "伊藤", "渡辺"])
```

1. 合格者の一覧を表示
2. 不合格者の一覧を表示
3. 合格者数と不合格者数を表示

### 期待される出力

```
全員の点数:
田中    85
鈴木    42
佐藤    78
高橋    55
伊藤    90
渡辺    63
dtype: int64

合格者:
田中    85
佐藤    78
伊藤    90
渡辺    63
dtype: int64

不合格者:
鈴木    42
高橋    55
dtype: int64

合格者数: 4
不合格者数: 2
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループでフィルタリング**

```python
import pandas as pd

scores = pd.Series([85, 42, 78, 55, 90, 63],
                   index=["田中", "鈴木", "佐藤", "高橋", "伊藤", "渡辺"])

print("全員の点数:")
print(scores)

# 合格者を抽出
passed = {}
failed = {}
for name in scores.index:
    if scores[name] >= 60:
        passed[name] = scores[name]
    else:
        failed[name] = scores[name]

print("\n合格者:")
print(pd.Series(passed))

print("\n不合格者:")
print(pd.Series(failed))

print("\n合格者数:", len(passed))
print("不合格者数:", len(failed))
```

**発展解法: ブールインデックスを使用**

```python
import pandas as pd

scores = pd.Series([85, 42, 78, 55, 90, 63],
                   index=["田中", "鈴木", "佐藤", "高橋", "伊藤", "渡辺"])

print("全員の点数:")
print(scores)

passed = scores[scores >= 60]
failed = scores[scores < 60]

print("\n合格者:")
print(passed)

print("\n不合格者:")
print(failed)

print("\n合格者数:", len(passed))
print("不合格者数:", len(failed))
```

</details>

---

## 応用問題4: 売上の前月比計算

### 問題

今月と先月の売上を比較し、成長率を計算してください。

```python
import pandas as pd

this_month = pd.Series([120, 80, 150, 200],
                       index=["商品A", "商品B", "商品C", "商品D"])
last_month = pd.Series([100, 100, 120, 180],
                       index=["商品A", "商品B", "商品C", "商品D"])
```

成長率 = (今月 - 先月) / 先月 × 100

### 期待される出力

```
今月の売上:
商品A    120
商品B     80
商品C    150
商品D    200
dtype: int64

先月の売上:
商品A    100
商品B    100
商品C    120
商品D    180
dtype: int64

成長率（%）:
商品A    20.0
商品B   -20.0
商品C    25.0
商品D    11.1
dtype: float64
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで計算**

```python
import pandas as pd

this_month = pd.Series([120, 80, 150, 200],
                       index=["商品A", "商品B", "商品C", "商品D"])
last_month = pd.Series([100, 100, 120, 180],
                       index=["商品A", "商品B", "商品C", "商品D"])

print("今月の売上:")
print(this_month)

print("\n先月の売上:")
print(last_month)

# 成長率を計算
growth = {}
for product in this_month.index:
    rate = (this_month[product] - last_month[product]) / last_month[product] * 100
    growth[product] = round(rate, 1)

print("\n成長率（%）:")
print(pd.Series(growth))
```

**発展解法: Series の演算を使用**

```python
import pandas as pd

this_month = pd.Series([120, 80, 150, 200],
                       index=["商品A", "商品B", "商品C", "商品D"])
last_month = pd.Series([100, 100, 120, 180],
                       index=["商品A", "商品B", "商品C", "商品D"])

print("今月の売上:")
print(this_month)

print("\n先月の売上:")
print(last_month)

growth = ((this_month - last_month) / last_month * 100).round(1)
print("\n成長率（%）:")
print(growth)
```

</details>

---

## 応用問題5: 偏差の計算

### 問題

各データが平均からどれだけ離れているか（偏差）を計算してください。

偏差 = 値 - 平均

```python
import pandas as pd

scores = pd.Series([72, 85, 90, 68, 75],
                   index=["国語", "数学", "英語", "理科", "社会"])
```

### 期待される出力

```
点数:
国語    72
数学    85
英語    90
理科    68
社会    75
dtype: int64

平均: 78.0

偏差（平均との差）:
国語    -6.0
数学     7.0
英語    12.0
理科   -10.0
社会    -3.0
dtype: float64
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで計算**

```python
import pandas as pd

scores = pd.Series([72, 85, 90, 68, 75],
                   index=["国語", "数学", "英語", "理科", "社会"])

print("点数:")
print(scores)

# 平均を計算
total = 0
for s in scores:
    total = total + s
mean = total / len(scores)
print("\n平均:", mean)

# 偏差を計算
deviation = {}
for subject in scores.index:
    deviation[subject] = scores[subject] - mean

print("\n偏差（平均との差）:")
print(pd.Series(deviation))
```

**発展解法: Series の演算を使用**

```python
import pandas as pd

scores = pd.Series([72, 85, 90, 68, 75],
                   index=["国語", "数学", "英語", "理科", "社会"])

print("点数:")
print(scores)

mean = scores.mean()
print("\n平均:", mean)

deviation = scores - mean
print("\n偏差（平均との差）:")
print(deviation)
```

</details>

---

## 🏆 チャレンジ問題: Series の正規化

### 問題

Series のデータを 0〜1 の範囲に正規化（Min-Max正規化）してください。

正規化の公式: $正規化値 = \frac{値 - 最小値}{最大値 - 最小値}$

```python
import pandas as pd

data = pd.Series([150, 200, 100, 250, 175],
                 index=["A", "B", "C", "D", "E"])
```

### 期待される出力

```
元のデータ:
A    150
B    200
C    100
D    250
E    175
dtype: int64

最小値: 100
最大値: 250

正規化後（0〜1）:
A    0.333
B    0.667
C    0.000
D    1.000
E    0.500
dtype: float64
```

<details>
<summary>解答例を見る</summary>

**基本解法: for ループで計算**

```python
import pandas as pd

data = pd.Series([150, 200, 100, 250, 175],
                 index=["A", "B", "C", "D", "E"])

print("元のデータ:")
print(data)

# 最小値と最大値を手計算
min_val = data[0]
max_val = data[0]
for v in data:
    if v < min_val:
        min_val = v
    if v > max_val:
        max_val = v

print("\n最小値:", min_val)
print("最大値:", max_val)

# 正規化を計算
normalized = {}
for label in data.index:
    normalized[label] = round((data[label] - min_val) / (max_val - min_val), 3)

print("\n正規化後（0〜1）:")
print(pd.Series(normalized))
```

**発展解法: Series の演算を使用**

```python
import pandas as pd

data = pd.Series([150, 200, 100, 250, 175],
                 index=["A", "B", "C", "D", "E"])

print("元のデータ:")
print(data)

min_val = data.min()
max_val = data.max()
print("\n最小値:", min_val)
print("最大値:", max_val)

normalized = ((data - min_val) / (max_val - min_val)).round(3)
print("\n正規化後（0〜1）:")
print(normalized)
```

</details>

---

おめでとうございます！🎉 Section1 を完了しました！
次は Section2: DataFrame（2次元データ）に進みましょう！
