# Section3: ミニプロジェクト② ペンギン観測データ分析 - 技術説明

## 🎯 このセクションの目標

南極で観測されたペンギンのデータを分析し、種類ごとの特徴や生息地の傾向を明らかにする。
Section1 で学んだ **ミニ分析プロセス（5ステップ）** を自力で実践する。

---

## 📋 プロジェクト概要

### シナリオ

あなたは野生動物研究所のデータ分析担当者です。
研究員から「ペンギン3種の特徴を分析して、種の判別に使える指標を見つけてほしい」と依頼されました。

手元にあるのは、南極の3つの島で観測されたペンギンの体の測定データです。
このデータを分析して、種類ごとの特徴と判別のポイントをまとめてください。

### 使用データ

**penguins データセット**（seaborn 組み込み）

| 列名 | 説明 | データ型 |
|------|------|----------|
| species | 種類（Adelie, Chinstrap, Gentoo） | カテゴリ |
| island | 島（Biscoe, Dream, Torgersen） | カテゴリ |
| bill_length_mm | くちばしの長さ（mm） | 数値 |
| bill_depth_mm | くちばしの深さ（mm） | 数値 |
| flipper_length_mm | ひれの長さ（mm） | 数値 |
| body_mass_g | 体重（g） | 数値 |
| sex | 性別（Male, Female） | カテゴリ |

```python
import seaborn as sns
df = sns.load_dataset('penguins')
```

> ⚠️ **注意**: このデータセットには欠損値があります。分析前に処理が必要です。

---

## 🔍 分析の問い（例）

このプロジェクトでは、以下のような問いに答えることを目指します：

| # | 分析の問い | 使う技術 |
|---|------------|----------|
| 1 | 種類によって体の大きさに違いはあるか？ | groupby + 棒グラフ |
| 2 | 種類ごとにどの島に生息しているか？ | クロス集計 + 棒グラフ |
| 3 | くちばしの形状で種類を区別できるか？ | 散布図（色分け） |
| 4 | 性別によって体格に差はあるか？ | groupby + 比較 |
| 5 | 種類を判別するのに最も有効な指標は何か？ | 総合的な考察 |

---

## 📊 分析で使うテクニック

### 1. 欠損値の処理

```python
# 欠損値の確認
print(df.isnull().sum())

# 欠損値を含む行を削除
df_clean = df.dropna()
print(f"処理前: {len(df)}行 → 処理後: {len(df_clean)}行")
```

### 2. 種類別の集計

```python
# 種類別の平均値
species_stats = df_clean.groupby('species')[['bill_length_mm', 'bill_depth_mm', 
                                              'flipper_length_mm', 'body_mass_g']].mean()
print(species_stats)

# 種類別の統計量（複数の集計を同時に）
species_summary = df_clean.groupby('species')['body_mass_g'].agg(['mean', 'std', 'min', 'max'])
print(species_summary)
```

### 3. クロス集計（種類 × 島）

```python
# 種類と島のクロス集計（件数）
cross_count = pd.crosstab(df_clean['species'], df_clean['island'])
print(cross_count)

# 割合で表示
cross_pct = pd.crosstab(df_clean['species'], df_clean['island'], normalize='index')
print(cross_pct.round(3))
```

### 4. 複数条件でのグループ化

```python
# 種類 × 性別 の平均体重
species_sex = df_clean.groupby(['species', 'sex'])['body_mass_g'].mean()
print(species_sex)

# 表形式に変換
species_sex_table = species_sex.unstack()
print(species_sex_table)
```

### 5. 相関分析

```python
# 数値列のみを選択
numeric_cols = ['bill_length_mm', 'bill_depth_mm', 'flipper_length_mm', 'body_mass_g']

# 相関行列
correlation_matrix = df_clean[numeric_cols].corr()
print(correlation_matrix.round(3))
```

---

## 📈 可視化のテクニック

### 種類別の比較（棒グラフ）

```python
import matplotlib.pyplot as plt

# 種類別の平均体重
species_weight = df_clean.groupby('species')['body_mass_g'].mean()

plt.figure(figsize=(8, 5))
species_weight.plot(kind='bar', color=['steelblue', 'coral', 'seagreen'])
plt.title('Average Body Mass by Species')
plt.xlabel('Species')
plt.ylabel('Body Mass (g)')
plt.xticks(rotation=0)
plt.tight_layout()
plt.show()
```

### 種類別の散布図（色分け）

```python
# 種類ごとに色を変えて散布図を作成
colors = {'Adelie': 'steelblue', 'Chinstrap': 'coral', 'Gentoo': 'seagreen'}

plt.figure(figsize=(8, 6))
for species in df_clean['species'].unique():
    subset = df_clean[df_clean['species'] == species]
    plt.scatter(subset['bill_length_mm'], subset['bill_depth_mm'], 
                label=species, alpha=0.7, color=colors[species])

plt.title('Bill Length vs Bill Depth by Species')
plt.xlabel('Bill Length (mm)')
plt.ylabel('Bill Depth (mm)')
plt.legend()
plt.tight_layout()
plt.show()
```

### クロス集計の可視化（積み上げ棒グラフ）

```python
# 種類 × 島 のクロス集計
cross_count = pd.crosstab(df_clean['species'], df_clean['island'])

cross_count.plot(kind='bar', stacked=True, figsize=(8, 5))
plt.title('Species Distribution by Island')
plt.xlabel('Species')
plt.ylabel('Count')
plt.xticks(rotation=0)
plt.legend(title='Island')
plt.tight_layout()
plt.show()
```

### 複数の変数を比較（複数サブプロット）

```python
fig, axes = plt.subplots(2, 2, figsize=(12, 10))

# 体重
df_clean.groupby('species')['body_mass_g'].mean().plot(kind='bar', ax=axes[0, 0], color='steelblue')
axes[0, 0].set_title('Body Mass')
axes[0, 0].set_ylabel('g')

# くちばしの長さ
df_clean.groupby('species')['bill_length_mm'].mean().plot(kind='bar', ax=axes[0, 1], color='coral')
axes[0, 1].set_title('Bill Length')
axes[0, 1].set_ylabel('mm')

# くちばしの深さ
df_clean.groupby('species')['bill_depth_mm'].mean().plot(kind='bar', ax=axes[1, 0], color='seagreen')
axes[1, 0].set_title('Bill Depth')
axes[1, 0].set_ylabel('mm')

# ひれの長さ
df_clean.groupby('species')['flipper_length_mm'].mean().plot(kind='bar', ax=axes[1, 1], color='purple')
axes[1, 1].set_title('Flipper Length')
axes[1, 1].set_ylabel('mm')

plt.tight_layout()
plt.show()
```

---

## 📝 レポートの書き方

### 構成例

```markdown
# ペンギン観測データ分析レポート

## 1. 分析の目的
ペンギン3種の体の特徴を分析し、種の判別に有効な指標を見つける。

## 2. 使用データ
- データセット: penguins（seaborn 組み込み）
- 元のレコード数: 344件
- 欠損値処理後: 333件
- 観測地: Biscoe島、Dream島、Torgersen島

## 3. 分析結果

### 3.1 種類別の体格比較
（グラフと数値を示し、発見を記述）

### 3.2 種類と生息地の関係
（クロス集計とグラフを示し、発見を記述）

### 3.3 くちばしの形状による種の判別
（散布図を示し、発見を記述）

## 4. 考察・結論
（種を判別するための有効な指標と、その理由）
```

---

## 🔬 このプロジェクトのポイント

| ポイント | 説明 |
|----------|------|
| 欠損値の処理 | penguins には欠損値があるため、最初に処理が必要 |
| カテゴリデータの分析 | 種類・島・性別など、カテゴリ変数が多い |
| 多変量の比較 | 4つの数値変数を比較する必要がある |
| 種類別の色分け | 散布図では種類ごとに色を変えると見やすい |

---

## ⏰ 想定作業時間

| 作業 | 時間 |
|------|------|
| データの読み込みと欠損値処理 | 10 分 |
| 種類別の集計と可視化 | 40 分 |
| 散布図による分析 | 20 分 |
| 結果の解釈とレポート作成 | 20 分 |
| **合計** | **90 分** |

---

次の「演習.md」で、このプロジェクトを実践します。
Section1 の応用問題と似た内容ですが、より詳細な分析に挑戦します！
