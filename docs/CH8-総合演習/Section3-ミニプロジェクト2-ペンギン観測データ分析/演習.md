# Section3: ミニプロジェクト② ペンギン観測データ分析 - 演習

## 🎯 この演習の目標

penguins データセットを使って、ペンギン観測データ分析プロジェクトを完成させる。
Section2 の売上分析よりも **自分で考える部分** を増やしています。

---

## 📋 プロジェクトの流れ

ミニ分析プロセスの5ステップに沿って進めます：

1. **問いを立てる** → 何を知りたいか決める
2. **データを見る** → head/info/describe で確認
3. **データを整える** → 欠損値処理
4. **集計・可視化する** → 分析を実行
5. **結論をまとめる** → レポートを作成

---

## 📝 Step 1: 問いを立てる

以下の問いに答える分析を行います：

1. **種類によって体の大きさ（体重、くちばし、ひれ）に違いはあるか？**
2. **種類ごとにどの島に生息しているか？**
3. **くちばしの形状（長さ×深さ）で種類を区別できるか？**
4. **性別によって体格に差はあるか？**
5. **種類を判別するのに最も有効な指標は何か？**

```python
# Jupyter の Markdown セルにメモ

# 分析の問い:
# 1. 種類によって体の大きさに違いはあるか？
# 2. 種類ごとにどの島に生息しているか？
# 3. くちばしの形状で種類を区別できるか？
# 4. 性別によって体格に差はあるか？
# 5. 種類を判別するのに最も有効な指標は何か？
```

---

## 📝 Step 2: データを見る

### 課題2-1: データの読み込みと確認

```python
# ライブラリのインポート
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

# penguins データセットの読み込み
df = sns.load_dataset('penguins')

# 先頭5行を表示
df.head()
```

```python
# データの基本情報
df.info()
```

```python
# 統計量
df.describe()
```

```python
# カテゴリ列の値を確認
print("=== 種類 ===")
print(df['species'].value_counts())
print("\n=== 島 ===")
print(df['island'].value_counts())
print("\n=== 性別 ===")
print(df['sex'].value_counts())
```

### 確認ポイント

- [ ] データは何行何列か？
- [ ] 欠損値はどの列に何件あるか？
- [ ] ペンギンの種類は何種類か？それぞれ何羽か？
- [ ] 観測された島は何箇所か？

<details>
<summary>💡 確認結果</summary>

- **344行、7列**
- **欠損値**: bill_length_mm(2), bill_depth_mm(2), flipper_length_mm(2), body_mass_g(2), sex(11)
- **3種類**: Adelie(152羽), Gentoo(124羽), Chinstrap(68羽)
- **3島**: Biscoe, Dream, Torgersen

</details>

---

## 📝 Step 3: データを整える

### 課題3-1: 欠損値の処理

```python
# 欠損値の確認
print("=== 欠損値の数 ===")
print(df.isnull().sum())

# 欠損値を含む行を削除
df_clean = df.dropna()

# 処理後の確認
print(f"\n処理前: {len(df)}行")
print(f"処理後: {len(df_clean)}行")
print(f"削除: {len(df) - len(df_clean)}行")
```

<details>
<summary>💡 確認</summary>

- 処理前: 344行
- 処理後: 333行
- 削除: 11行

</details>

---

## 📝 Step 4: 集計・可視化する

### 課題4-1: 種類別の体格比較

#### 4-1-1: 種類別の平均値を計算

```python
# 種類別の平均値（すべての数値列）
species_stats = df_clean.groupby('species')[['bill_length_mm', 'bill_depth_mm', 
                                              'flipper_length_mm', 'body_mass_g']].mean()
print(species_stats.round(2))
```

<details>
<summary>💡 結果の解釈</summary>

| 種類 | くちばし長さ | くちばし深さ | ひれ長さ | 体重 |
|------|-------------|-------------|---------|------|
| Adelie | 38.82 mm | 18.35 mm | 190.10 mm | 3,706 g |
| Chinstrap | 48.83 mm | 18.42 mm | 195.82 mm | 3,733 g |
| Gentoo | 47.57 mm | 15.00 mm | 217.24 mm | 5,092 g |

- **Gentoo** が最も体重が重く、ひれが長い
- **Adelie** はくちばしが短い
- **Chinstrap** は Adelie とサイズが近いが、くちばしが長い

</details>

#### 4-1-2: 種類別の体重を棒グラフで可視化

```python
# 種類別の平均体重
species_weight = df_clean.groupby('species')['body_mass_g'].mean()

plt.figure(figsize=(8, 5))
# ここにコードを書く（棒グラフ）

plt.title('Average Body Mass by Species')
plt.xlabel('Species')
plt.ylabel('Body Mass (g)')
plt.xticks(rotation=0)
plt.tight_layout()
plt.show()
```

<details>
<summary>💡 解答を見る</summary>

```python
# 種類別の平均体重
species_weight = df_clean.groupby('species')['body_mass_g'].mean()

plt.figure(figsize=(8, 5))
species_weight.plot(kind='bar', color=['steelblue', 'coral', 'seagreen'])
plt.title('Average Body Mass by Species')
plt.xlabel('Species')
plt.ylabel('Body Mass (g)')
plt.xticks(rotation=0)
plt.tight_layout()
plt.show()
```

</details>

#### 4-1-3: 4つの指標を比較（複数グラフ）

```python
# 4つの指標を一度に比較
fig, axes = plt.subplots(2, 2, figsize=(12, 10))

# ここに4つのグラフを作成するコードを書く
# axes[0, 0]: 体重
# axes[0, 1]: くちばしの長さ
# axes[1, 0]: くちばしの深さ
# axes[1, 1]: ひれの長さ

plt.tight_layout()
plt.show()
```

<details>
<summary>💡 解答を見る</summary>

```python
fig, axes = plt.subplots(2, 2, figsize=(12, 10))

colors = ['steelblue', 'coral', 'seagreen']

# 体重
df_clean.groupby('species')['body_mass_g'].mean().plot(
    kind='bar', ax=axes[0, 0], color=colors)
axes[0, 0].set_title('Body Mass')
axes[0, 0].set_ylabel('g')
axes[0, 0].set_xticklabels(axes[0, 0].get_xticklabels(), rotation=0)

# くちばしの長さ
df_clean.groupby('species')['bill_length_mm'].mean().plot(
    kind='bar', ax=axes[0, 1], color=colors)
axes[0, 1].set_title('Bill Length')
axes[0, 1].set_ylabel('mm')
axes[0, 1].set_xticklabels(axes[0, 1].get_xticklabels(), rotation=0)

# くちばしの深さ
df_clean.groupby('species')['bill_depth_mm'].mean().plot(
    kind='bar', ax=axes[1, 0], color=colors)
axes[1, 0].set_title('Bill Depth')
axes[1, 0].set_ylabel('mm')
axes[1, 0].set_xticklabels(axes[1, 0].get_xticklabels(), rotation=0)

# ひれの長さ
df_clean.groupby('species')['flipper_length_mm'].mean().plot(
    kind='bar', ax=axes[1, 1], color=colors)
axes[1, 1].set_title('Flipper Length')
axes[1, 1].set_ylabel('mm')
axes[1, 1].set_xticklabels(axes[1, 1].get_xticklabels(), rotation=0)

plt.tight_layout()
plt.show()
```

</details>

---

### 課題4-2: 種類と島の関係

#### 4-2-1: クロス集計

```python
# 種類 × 島 のクロス集計（件数）
cross_count = pd.crosstab(df_clean['species'], df_clean['island'])
print(cross_count)
```

<details>
<summary>💡 結果の解釈</summary>

| 種類 | Biscoe | Dream | Torgersen |
|------|--------|-------|-----------|
| Adelie | 44 | 55 | 47 |
| Chinstrap | 0 | 68 | 0 |
| Gentoo | 119 | 0 | 0 |

- **Adelie** は3つの島すべてに生息
- **Chinstrap** は Dream 島のみに生息
- **Gentoo** は Biscoe 島のみに生息

</details>

#### 4-2-2: 積み上げ棒グラフで可視化

```python
# クロス集計を積み上げ棒グラフで可視化
# ここにコードを書く
```

<details>
<summary>💡 解答を見る</summary>

```python
# クロス集計を積み上げ棒グラフで可視化
cross_count = pd.crosstab(df_clean['species'], df_clean['island'])

cross_count.plot(kind='bar', stacked=True, figsize=(8, 5), 
                 color=['steelblue', 'coral', 'seagreen'])
plt.title('Species Distribution by Island')
plt.xlabel('Species')
plt.ylabel('Count')
plt.xticks(rotation=0)
plt.legend(title='Island')
plt.tight_layout()
plt.show()
```

</details>

---

### 課題4-3: くちばしの形状による種の判別

#### 4-3-1: くちばしの長さ×深さの散布図（種類別に色分け）

```python
# 種類ごとに色を変えて散布図を作成
colors = {'Adelie': 'steelblue', 'Chinstrap': 'coral', 'Gentoo': 'seagreen'}

plt.figure(figsize=(8, 6))

# ここにコードを書く（for ループで種類ごとにプロット）

plt.title('Bill Length vs Bill Depth by Species')
plt.xlabel('Bill Length (mm)')
plt.ylabel('Bill Depth (mm)')
plt.legend()
plt.tight_layout()
plt.show()
```

<details>
<summary>💡 ヒントを見る</summary>

```python
for species in df_clean['species'].unique():
    subset = df_clean[df_clean['species'] == species]
    plt.scatter(subset['x列'], subset['y列'], 
                label=species, alpha=0.7, color=colors[species])
```

</details>

<details>
<summary>💡 解答を見る</summary>

```python
colors = {'Adelie': 'steelblue', 'Chinstrap': 'coral', 'Gentoo': 'seagreen'}

plt.figure(figsize=(8, 6))
for species in df_clean['species'].unique():
    subset = df_clean[df_clean['species'] == species]
    plt.scatter(subset['bill_length_mm'], subset['bill_depth_mm'], 
                label=species, alpha=0.7, color=colors[species])

plt.title('Bill Length vs Bill Depth by Species')
plt.xlabel('Bill Length (mm)')
plt.ylabel('Bill Depth (mm)')
plt.legend()
plt.tight_layout()
plt.show()
```

**発見**: 3種類が明確に分離している！
- Adelie: 左上（短い＋深い）
- Chinstrap: 右上（長い＋深い）
- Gentoo: 右下（長い＋浅い）

</details>

#### 4-3-2: 体重×ひれの長さの散布図

```python
# 体重とひれの長さの関係も確認
plt.figure(figsize=(8, 6))

# ここにコードを書く

plt.title('Body Mass vs Flipper Length by Species')
plt.xlabel('Body Mass (g)')
plt.ylabel('Flipper Length (mm)')
plt.legend()
plt.tight_layout()
plt.show()
```

<details>
<summary>💡 解答を見る</summary>

```python
plt.figure(figsize=(8, 6))
for species in df_clean['species'].unique():
    subset = df_clean[df_clean['species'] == species]
    plt.scatter(subset['body_mass_g'], subset['flipper_length_mm'], 
                label=species, alpha=0.7, color=colors[species])

plt.title('Body Mass vs Flipper Length by Species')
plt.xlabel('Body Mass (g)')
plt.ylabel('Flipper Length (mm)')
plt.legend()
plt.tight_layout()
plt.show()
```

**発見**: Gentoo は右上に分離、Adelie と Chinstrap は重なる部分がある。

</details>

---

### 課題4-4: 性別による体格差

#### 4-4-1: 種類 × 性別 の平均体重

```python
# 種類 × 性別 の平均体重
species_sex = df_clean.groupby(['species', 'sex'])['body_mass_g'].mean().unstack()
print(species_sex.round(0))

# グラフで可視化
species_sex.plot(kind='bar', figsize=(8, 5))
plt.title('Average Body Mass by Species and Sex')
plt.xlabel('Species')
plt.ylabel('Body Mass (g)')
plt.xticks(rotation=0)
plt.legend(title='Sex')
plt.tight_layout()
plt.show()
```

<details>
<summary>💡 結果の解釈</summary>

| 種類 | Female | Male |
|------|--------|------|
| Adelie | 3,369 g | 4,043 g |
| Chinstrap | 3,527 g | 3,939 g |
| Gentoo | 4,680 g | 5,485 g |

- すべての種類で **オスがメスより重い**
- 差は約 400-800g（15-20%程度）

</details>

---

## 📝 Step 5: 結論をまとめる

### 課題5: 分析レポートを作成

以下のテンプレートに沿って、分析レポートを作成してください。

```markdown
# ペンギン観測データ分析レポート

## 1. 分析の目的
ペンギン3種の体の特徴を分析し、種の判別に有効な指標を見つける。

## 2. 使用データ
- データセット: penguins（seaborn 組み込み）
- 元のレコード数: 344件
- 欠損値処理後: 333件

## 3. 分析結果

### 3.1 種類別の体格比較
（ここに結果と発見を書く）

### 3.2 種類と生息地の関係
（ここに結果と発見を書く）

### 3.3 くちばしの形状による種の判別
（ここに結果と発見を書く）

### 3.4 性別による体格差
（ここに結果と発見を書く）

## 4. 結論：種の判別に有効な指標

### 最も有効な指標
（くちばしの長さ×深さ、体重×ひれの長さ など）

### 判別のポイント
（各種類の特徴をまとめる）
```

<details>
<summary>💡 レポート例を見る</summary>

```markdown
# ペンギン観測データ分析レポート

## 1. 分析の目的
ペンギン3種（Adelie, Chinstrap, Gentoo）の体の特徴を分析し、
種の判別に有効な指標を見つける。

## 2. 使用データ
- データセット: penguins（seaborn 組み込み）
- 元のレコード数: 344件
- 欠損値処理後: 333件
- 観測地: Biscoe島、Dream島、Torgersen島

## 3. 分析結果

### 3.1 種類別の体格比較
- **Gentoo** が最も大きい（体重 5,092g、ひれ 217mm）
- **Adelie と Chinstrap** はサイズが近い（体重 約3,700g）
- くちばしの長さは Chinstrap > Gentoo > Adelie

### 3.2 種類と生息地の関係
- **Adelie**: 3つの島すべてに生息（唯一の全島生息種）
- **Chinstrap**: Dream 島のみに生息
- **Gentoo**: Biscoe 島のみに生息
- 生息島で種を特定できるケースがある

### 3.3 くちばしの形状による種の判別
くちばしの「長さ × 深さ」の散布図で3種類が明確に分離する：
- **Adelie**: 短い（38mm）+ 深い（18mm）→ 左上
- **Chinstrap**: 長い（49mm）+ 深い（18mm）→ 右上
- **Gentoo**: 長い（48mm）+ 浅い（15mm）→ 右下

これは種の判別に最も有効な指標である。

### 3.4 性別による体格差
- すべての種でオスがメスより 15-20% 重い
- Gentoo は性差が最も大きい（約 800g 差）

## 4. 結論：種の判別に有効な指標

### 最も有効な指標
1. **くちばしの長さ × 深さ**（散布図で3種が完全に分離）
2. **体重 × ひれの長さ**（Gentoo を判別しやすい）

### 判別のポイント
| 種類 | 特徴 |
|------|------|
| Adelie | くちばしが短くて深い。3島に生息 |
| Chinstrap | くちばしが長くて深い。Dream島のみ |
| Gentoo | 体が大きく、くちばしが長くて浅い。Biscoe島のみ |

くちばしの形状を測定すれば、高い精度で種を判別できる。
```

</details>

---

## ✅ 演習の完了チェック

以下の項目ができたら、この演習は完了です：

- [ ] penguins データセットを読み込み、内容を確認した
- [ ] 欠損値を処理した
- [ ] 種類別の体格（4指標）を比較した
- [ ] 種類と島のクロス集計を行った
- [ ] くちばしの散布図（種類別色分け）を作成した
- [ ] 性別による体格差を分析した
- [ ] 分析レポートを作成した

---

次は「応用問題」で、より発展的な分析に挑戦します！
