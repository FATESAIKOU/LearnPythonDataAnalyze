# Section1: 応用問題

## 🚀 この応用問題について

関数を使った実践的なプログラムに挑戦しましょう。

---

## 応用問題1: パスワード強度チェッカー

### 問題

パスワードの強度を判定する関数 `check_password_strength` を作成してください。

**判定基準:**
- 8文字未満 → 「弱い」
- 8文字以上12文字未満 → 「普通」
- 12文字以上 → 「強い」

### 実行例

```python
print(check_password_strength("abc"))
print(check_password_strength("password123"))
print(check_password_strength("mySecurePassword2024"))
```

### 期待される出力

```
弱い
普通
強い
```

<details>
<summary>解答例を見る</summary>

```python
def check_password_strength(password):
    """パスワードの強度を判定する"""
    length = len(password)
    
    if length < 8:
        return "弱い"
    elif length < 12:
        return "普通"
    else:
        return "強い"

print(check_password_strength("abc"))
print(check_password_strength("password123"))
print(check_password_strength("mySecurePassword2024"))
```

</details>

---

## 応用問題2: 税込価格計算

### 問題

商品名と税抜価格を受け取って、レシート風の文字列を返す関数 `create_receipt` を作成してください。

**仕様:**
- 税率は 10%
- 税込価格は整数に切り捨て

### 実行例

```python
print(create_receipt("りんご", 150))
print(create_receipt("牛乳", 198))
```

### 期待される出力

```
りんご: 150円 (税込 165円)
牛乳: 198円 (税込 217円)
```

<details>
<summary>解答例を見る</summary>

```python
def create_receipt(item_name, price):
    """商品のレシート情報を作成する"""
    tax_included = int(price * 1.10)
    return item_name + ": " + str(price) + "円 (税込 " + str(tax_included) + "円)"

print(create_receipt("りんご", 150))
print(create_receipt("牛乳", 198))
```

</details>

---

## 応用問題3: リストの統計情報

### 問題

数値のリストを受け取って、最小値、最大値、平均値を返す関数 `calc_statistics` を作成してください。

### 実行例

```python
scores = [85, 72, 90, 68, 95]
min_val, max_val, avg_val = calc_statistics(scores)
print("最小:", min_val)
print("最大:", max_val)
print("平均:", avg_val)
```

### 期待される出力

```
最小: 68
最大: 95
平均: 82.0
```

<details>
<summary>解答例を見る</summary>

```python
def calc_statistics(numbers):
    """
    リストの統計情報を計算する
    
    Returns:
        最小値, 最大値, 平均値
    """
    min_val = numbers[0]
    max_val = numbers[0]
    total = 0
    
    for num in numbers:
        if num < min_val:
            min_val = num
        if num > max_val:
            max_val = num
        total = total + num
    
    avg_val = total / len(numbers)
    
    return min_val, max_val, avg_val

scores = [85, 72, 90, 68, 95]
min_val, max_val, avg_val = calc_statistics(scores)
print("最小:", min_val)
print("最大:", max_val)
print("平均:", avg_val)
```

</details>

---

## 応用問題4: 文字列フォーマット

### 問題

名前と年齢を受け取って、自己紹介文を返す関数 `create_introduction` を作成してください。

**仕様:**
- 名前の前後にスペースがあれば削除
- 年齢に応じてメッセージを変える

### 実行例

```python
print(create_introduction("  田中  ", 25))
print(create_introduction("佐藤", 65))
```

### 期待される出力

```
こんにちは！田中です。25歳の若者です。
こんにちは！佐藤です。65歳のベテランです。
```

**判定基準:**
- 40歳未満: 「若者」
- 40歳以上60歳未満: 「中堅」
- 60歳以上: 「ベテラン」

<details>
<summary>解答例を見る</summary>

```python
def create_introduction(name, age):
    """自己紹介文を作成する"""
    # 名前の前後のスペースを削除
    clean_name = name.strip()
    
    # 年齢に応じたラベル
    if age < 40:
        label = "若者"
    elif age < 60:
        label = "中堅"
    else:
        label = "ベテラン"
    
    return "こんにちは！" + clean_name + "です。" + str(age) + "歳の" + label + "です。"

print(create_introduction("  田中  ", 25))
print(create_introduction("佐藤", 65))
```

</details>

---

## 🏆 チャレンジ問題1: じゃんけん判定

### 問題

じゃんけんの勝敗を判定する関数 `judge_janken` を作成してください。

**仕様:**
- 入力: プレイヤーの手、コンピュータの手（"グー", "チョキ", "パー"）
- 出力: "勝ち", "負け", "あいこ"

### 実行例

```python
print(judge_janken("グー", "チョキ"))
print(judge_janken("パー", "グー"))
print(judge_janken("チョキ", "チョキ"))
print(judge_janken("グー", "パー"))
```

### 期待される出力

```
勝ち
勝ち
あいこ
負け
```

<details>
<summary>解答例を見る</summary>

```python
def judge_janken(player, computer):
    """
    じゃんけんの勝敗を判定する
    
    Args:
        player: プレイヤーの手
        computer: コンピュータの手
    
    Returns:
        判定結果（勝ち/負け/あいこ）
    """
    if player == computer:
        return "あいこ"
    
    # 勝ちパターン
    if player == "グー" and computer == "チョキ":
        return "勝ち"
    if player == "チョキ" and computer == "パー":
        return "勝ち"
    if player == "パー" and computer == "グー":
        return "勝ち"
    
    return "負け"

print(judge_janken("グー", "チョキ"))
print(judge_janken("パー", "グー"))
print(judge_janken("チョキ", "チョキ"))
print(judge_janken("グー", "パー"))
```

</details>

---

## 🏆 チャレンジ問題2: 数値のフォーマット

### 問題

数値を3桁区切りでフォーマットする関数 `format_number` を作成してください。

**仕様:**
- 組み込み関数を使わずに実装する
- 負の数にも対応する

### 実行例

```python
print(format_number(1234567))
print(format_number(1000))
print(format_number(-9876543))
print(format_number(123))
```

### 期待される出力

```
1,234,567
1,000
-9,876,543
123
```

<details>
<summary>解答例を見る</summary>

```python
def format_number(n):
    """
    数値を3桁区切りでフォーマットする
    """
    # 負の数の処理
    if n < 0:
        return "-" + format_number(-n)
    
    # 文字列に変換
    s = str(n)
    result = ""
    count = 0
    
    # 後ろから処理
    index = len(s) - 1
    while index >= 0:
        if count > 0 and count % 3 == 0:
            result = "," + result
        result = s[index] + result
        count = count + 1
        index = index - 1
    
    return result

print(format_number(1234567))
print(format_number(1000))
print(format_number(-9876543))
print(format_number(123))
```

</details>

---

## 🎯 応用問題のまとめ

これらの問題を通して：

- 実用的な関数の設計方法
- 複数の戻り値の活用
- 文字列処理と関数の組み合わせ
- 条件分岐を含む関数の作成

を学びました！

---

## 🚀 次のステップ

Section2 では、デフォルト引数やラムダ式など、より高度な関数の機能を学びます！
