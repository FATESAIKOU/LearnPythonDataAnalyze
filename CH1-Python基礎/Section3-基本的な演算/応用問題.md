# Section3: 応用問題

## 🚀 この応用問題について

計算と文字列操作を組み合わせた実践的な問題です。

---

## 応用問題1: 買い物計算プログラム

### 問題

以下の買い物リストの合計金額と、10%の消費税を含めた総額を計算してください。

| 商品 | 単価 | 個数 |
|------|------|------|
| りんご | 120円 | 5個 |
| バナナ | 200円 | 3個 |
| みかん | 80円 | 10個 |

### 出力例

```
===== 買い物レシート =====
りんご 120円 × 5個 = 600円
バナナ 200円 × 3個 = 600円
みかん 80円 × 10個 = 800円
--------------------------
小計: 2000円
消費税(10%): 200円
合計: 2200円
===========================
```

<details>
<summary>解答例を見る</summary>

```python
# 単価
apple_price = 120
banana_price = 200
orange_price = 80

# 個数
apple_count = 5
banana_count = 3
orange_count = 10

# 小計
apple_total = apple_price * apple_count
banana_total = banana_price * banana_count
orange_total = orange_price * orange_count

# 合計計算
subtotal = apple_total + banana_total + orange_total
tax = int(subtotal * 0.1)
total = subtotal + tax

# 表示
print("===== 買い物レシート =====")
print("りんご " + str(apple_price) + "円 × " + str(apple_count) + "個 = " + str(apple_total) + "円")
print("バナナ " + str(banana_price) + "円 × " + str(banana_count) + "個 = " + str(banana_total) + "円")
print("みかん " + str(orange_price) + "円 × " + str(orange_count) + "個 = " + str(orange_total) + "円")
print("--------------------------")
print("小計: " + str(subtotal) + "円")
print("消費税(10%): " + str(tax) + "円")
print("合計: " + str(total) + "円")
print("===========================")
```

</details>

---

## 応用問題2: 温度変換プログラム

### 問題

摂氏温度を華氏温度に変換するプログラムを作成してください。

**変換式:** 華氏 = 摂氏 × 9 ÷ 5 + 32

以下の摂氏温度を変換してください：
- 0度
- 25度
- 100度

### 出力例

```
===== 温度変換 =====
摂氏 0度 → 華氏 32.0度
摂氏 25度 → 華氏 77.0度
摂氏 100度 → 華氏 212.0度
====================
```

<details>
<summary>解答例を見る</summary>

```python
print("===== 温度変換 =====")

celsius1 = 0
fahrenheit1 = celsius1 * 9 / 5 + 32
print("摂氏 " + str(celsius1) + "度 → 華氏 " + str(fahrenheit1) + "度")

celsius2 = 25
fahrenheit2 = celsius2 * 9 / 5 + 32
print("摂氏 " + str(celsius2) + "度 → 華氏 " + str(fahrenheit2) + "度")

celsius3 = 100
fahrenheit3 = celsius3 * 9 / 5 + 32
print("摂氏 " + str(celsius3) + "度 → 華氏 " + str(fahrenheit3) + "度")

print("====================")
```

</details>

---

## 応用問題3: 時間の変換

### 問題

秒数を「○時間○分○秒」の形式に変換してください。

例：`3725秒` → `1時間2分5秒`

以下の秒数を変換してください：
- 90秒
- 3600秒
- 7384秒

### ヒント

- 1時間 = 3600秒
- 1分 = 60秒
- 整数除算（//）と余り（%）を使います

<details>
<summary>解答例を見る</summary>

```python
print("===== 時間変換 =====")

# 90秒の変換
seconds1 = 90
hours1 = seconds1 // 3600
remaining1 = seconds1 % 3600
minutes1 = remaining1 // 60
secs1 = remaining1 % 60
print(str(seconds1) + "秒 → " + str(hours1) + "時間" + str(minutes1) + "分" + str(secs1) + "秒")

# 3600秒の変換
seconds2 = 3600
hours2 = seconds2 // 3600
remaining2 = seconds2 % 3600
minutes2 = remaining2 // 60
secs2 = remaining2 % 60
print(str(seconds2) + "秒 → " + str(hours2) + "時間" + str(minutes2) + "分" + str(secs2) + "秒")

# 7384秒の変換
seconds3 = 7384
hours3 = seconds3 // 3600
remaining3 = seconds3 % 3600
minutes3 = remaining3 // 60
secs3 = remaining3 % 60
print(str(seconds3) + "秒 → " + str(hours3) + "時間" + str(minutes3) + "分" + str(secs3) + "秒")

print("====================")
```

</details>

---

## 応用問題4: BMI 計算

### 問題

身長と体重から BMI を計算するプログラムを作成してください。

**BMI の計算式:** BMI = 体重(kg) ÷ 身長(m)²

### 入力データ

- 身長: 170cm
- 体重: 65kg

### 出力例

```
===== BMI 計算 =====
身長: 170cm (1.7m)
体重: 65kg
BMI: 22.49
====================
```

### ヒント

- cm を m に変換するには 100 で割る
- 累乗は `**` を使う

<details>
<summary>解答例を見る</summary>

```python
height_cm = 170
weight_kg = 65

# cm を m に変換
height_m = height_cm / 100

# BMI 計算
bmi = weight_kg / (height_m ** 2)

# 小数点2桁に丸める（後の章で詳しく学びます）
bmi = int(bmi * 100) / 100

print("===== BMI 計算 =====")
print("身長: " + str(height_cm) + "cm (" + str(height_m) + "m)")
print("体重: " + str(weight_kg) + "kg")
print("BMI: " + str(bmi))
print("====================")
```

</details>

---

## 応用問題5: 割り勘計算

### 問題

飲み会の会計を割り勘するプログラムを作成してください。

- 合計金額: 15800円
- 人数: 6人
- 一人あたりの金額を100円単位で切り上げ

### 出力例

```
===== 割り勘計算 =====
合計金額: 15800円
人数: 6人
------------------------
一人あたり（端数あり）: 2633円
一人あたり（100円単位）: 2700円
========================
```

### ヒント

100円単位に切り上げるには：
1. 金額を100で割る
2. 整数部分に1を足す（切り上げ）
3. 100を掛ける

<details>
<summary>解答例を見る</summary>

```python
total = 15800
people = 6

# 一人あたり（端数あり）
per_person = total // people

# 100円単位に切り上げ
per_person_rounded = ((per_person // 100) + 1) * 100

print("===== 割り勘計算 =====")
print("合計金額: " + str(total) + "円")
print("人数: " + str(people) + "人")
print("------------------------")
print("一人あたり（端数あり）: " + str(per_person) + "円")
print("一人あたり（100円単位）: " + str(per_person_rounded) + "円")
print("========================")
```

</details>

---

## 応用問題6: 簡易電卓

### 問題

ユーザーから2つの数値を入力してもらい、四則演算の結果を表示する簡易電卓を作成してください。

### 出力例

```
===== 簡易電卓 =====
1つ目の数字を入力: 10
2つ目の数字を入力: 3
--------------------
10 + 3 = 13
10 - 3 = 7
10 × 3 = 30
10 ÷ 3 = 3.3333333333333335
====================
```

<details>
<summary>解答例を見る</summary>

```python
print("===== 簡易電卓 =====")

num1 = input("1つ目の数字を入力: ")
num2 = input("2つ目の数字を入力: ")

num1 = int(num1)
num2 = int(num2)

print("--------------------")
print(str(num1) + " + " + str(num2) + " = " + str(num1 + num2))
print(str(num1) + " - " + str(num2) + " = " + str(num1 - num2))
print(str(num1) + " × " + str(num2) + " = " + str(num1 * num2))
print(str(num1) + " ÷ " + str(num2) + " = " + str(num1 / num2))
print("====================")
```

</details>

---

## 応用問題7: 文字列で装飾

### 問題

ユーザーが入力したメッセージを、以下のような枠で囲んで表示してください。

### 出力例

```
メッセージを入力: Hello Python!
********************
* Hello Python!    *
********************
```

### ヒント

- 枠の幅を20文字固定にする
- 文字列の長さに応じてスペースを調整

<details>
<summary>解答例を見る</summary>

```python
message = input("メッセージを入力: ")

# 枠の幅
width = 20

# 上下の枠線
border = "*" * width

# メッセージ行（スペースで埋める）
# 簡易版：固定幅で表示
content = "* " + message

# スペースで埋める（簡易版）
spaces_needed = width - len(content) - 1
spaces = " " * spaces_needed
content = content + spaces + "*"

print(border)
print(content)
print(border)
```

</details>

---

## 🏆 チャレンジ問題

### 問題: 預金利息計算

100万円を年利 0.5% で銀行に預けた場合、1年後、3年後、5年後の残高を計算してください。

**複利計算式:** 残高 = 元金 × (1 + 年利)^年数

### 出力例

```
===== 預金シミュレーション =====
元金: 1000000円
年利: 0.5%
--------------------------------
1年後: 1005000円
3年後: 1015075円
5年後: 1025251円
================================
```

### ヒント

- 年利 0.5% は `0.005` として計算
- 累乗は `**` を使う
- 小数点以下は切り捨て（int() を使う）

---

## 🎯 応用問題のまとめ

これらの問題を通して：

- 複数の計算を組み合わせる方法
- 実用的なプログラムの作成
- input() を使ったインタラクティブなプログラム
- 型変換の実践的な使い方

を学びました！

---

## 🎉 CH1 完了！

おめでとうございます！CH1「Python 基礎」を修了しました。

次は CH2「Python 制御構文」で、if 文やループを学びましょう！
