# Section3: 基本的な演算

## 🎯 このセクションの目標

**変数を使って計算や文字列操作ができるようになる！**

Python を電卓のように使いこなし、データを自由に操作できるようになりましょう。

---

## 🔢 算術演算子

Python で使える計算記号（演算子）を学びましょう。

| 演算子 | 意味 | 例 | 結果 |
|--------|------|-----|------|
| `+` | 足し算 | `5 + 3` | `8` |
| `-` | 引き算 | `10 - 4` | `6` |
| `*` | 掛け算 | `6 * 7` | `42` |
| `/` | 割り算 | `15 / 4` | `3.75` |
| `//` | 整数除算 | `15 // 4` | `3` |
| `%` | 余り | `15 % 4` | `3` |
| `**` | 累乗 | `2 ** 3` | `8` |

---

## ➕ 足し算と引き算

```python
# 足し算
result = 10 + 5
print(result)  # 15

# 引き算
result = 20 - 8
print(result)  # 12

# 変数同士の計算
a = 100
b = 30
print(a + b)  # 130
print(a - b)  # 70
```

---

## ✖️ 掛け算

```python
# 基本的な掛け算
result = 6 * 7
print(result)  # 42

# 変数を使った掛け算
price = 500
quantity = 3
total = price * quantity
print(total)  # 1500
```

---

## ➗ 割り算（3種類）

### 通常の割り算（/）

結果は常に小数（float）になります。

```python
result = 10 / 3
print(result)  # 3.3333333333333335

result = 10 / 2
print(result)  # 5.0（整数でも 5.0 になる）
```

### 整数除算（//）

小数点以下を切り捨てます。

```python
result = 10 // 3
print(result)  # 3

result = 17 // 5
print(result)  # 3
```

### 余り（%）

割り算の余りを求めます。

```python
result = 10 % 3
print(result)  # 1（10 ÷ 3 = 3 余り 1）

result = 17 % 5
print(result)  # 2（17 ÷ 5 = 3 余り 2）
```

### 余りの活用例

```python
# 偶数・奇数の判定に使える
number = 7
remainder = number % 2
print(remainder)  # 1（奇数なら1、偶数なら0）
```

---

## 🔼 累乗（**）

```python
# 2の3乗（2 × 2 × 2）
result = 2 ** 3
print(result)  # 8

# 10の2乗（10 × 10）
result = 10 ** 2
print(result)  # 100

# 平方根（0.5乗）
result = 9 ** 0.5
print(result)  # 3.0
```

---

## 📐 計算の優先順位

数学と同じルールです。

```python
# 掛け算・割り算が先
result = 2 + 3 * 4
print(result)  # 14（3 * 4 = 12、2 + 12 = 14）

# カッコで優先順位を変える
result = (2 + 3) * 4
print(result)  # 20（2 + 3 = 5、5 * 4 = 20）
```

### 優先順位

1. `()` カッコ
2. `**` 累乗
3. `*` `/` `//` `%` 掛け算・割り算
4. `+` `-` 足し算・引き算

---

## 📝 文字列の操作

### 文字列の連結（+）

```python
first_name = "田中"
last_name = "太郎"

full_name = first_name + last_name
print(full_name)  # 田中太郎

# スペースを入れる
full_name = first_name + " " + last_name
print(full_name)  # 田中 太郎
```

### 文字列の繰り返し（*）

```python
# 文字列を繰り返す
line = "-" * 20
print(line)  # --------------------

# 単語の繰り返し
word = "Hello! "
print(word * 3)  # Hello! Hello! Hello! 
```

---

## 🔄 型変換

異なる型のデータを変換できます。

### int()：整数に変換

```python
# 小数 → 整数（小数点以下切り捨て）
num = int(3.7)
print(num)  # 3

# 文字列 → 整数
num = int("100")
print(num)  # 100
print(type(num))  # <class 'int'>
```

### float()：小数に変換

```python
# 整数 → 小数
num = float(5)
print(num)  # 5.0

# 文字列 → 小数
num = float("3.14")
print(num)  # 3.14
```

### str()：文字列に変換

```python
# 数値 → 文字列
text = str(123)
print(text)  # 123
print(type(text))  # <class 'str'>

# 計算結果を文字列に
age = 25
message = "私は" + str(age) + "歳です"
print(message)  # 私は25歳です
```

---

## ⚠️ 型変換が必要な場面

### エラーになるケース

```python
age = 25
message = "私は" + age + "歳です"  # ❌ エラー！
```

**エラーメッセージ：**
```
TypeError: can only concatenate str (not "int") to str
```

### 解決方法

```python
age = 25
message = "私は" + str(age) + "歳です"  # ✅ OK！
print(message)  # 私は25歳です
```

---

## ⌨️ input() 関数

ユーザーからの入力を受け取ります。

```python
name = input("名前を入力してください: ")
print("こんにちは、" + name + "さん！")
```

**実行時：**
```
名前を入力してください: 太郎
こんにちは、太郎さん！
```

### 注意：input() は常に文字列を返す

```python
age = input("年齢を入力してください: ")
print(type(age))  # <class 'str'>
```

数値として使いたい場合は変換が必要：

```python
age = input("年齢を入力してください: ")
age = int(age)  # 整数に変換
next_year_age = age + 1
print("来年は" + str(next_year_age) + "歳ですね")
```

---

## 🔧 複合代入演算子

変数の値を更新する便利な書き方です。

| 演算子 | 意味 | 同じ意味 |
|--------|------|----------|
| `+=` | 加算して代入 | `a = a + b` |
| `-=` | 減算して代入 | `a = a - b` |
| `*=` | 乗算して代入 | `a = a * b` |
| `/=` | 除算して代入 | `a = a / b` |

```python
score = 100

score += 10  # score = score + 10 と同じ
print(score)  # 110

score -= 20  # score = score - 20 と同じ
print(score)  # 90

score *= 2   # score = score * 2 と同じ
print(score)  # 180
```

---

## 📊 実践例：買い物計算

```python
# 商品の価格
apple_price = 150
bread_price = 200
milk_price = 180

# 個数
apple_count = 3
bread_count = 2
milk_count = 1

# 小計
apple_total = apple_price * apple_count
bread_total = bread_price * bread_count
milk_total = milk_price * milk_count

# 合計
total = apple_total + bread_total + milk_total
print("合計金額:")
print(total)

# 税込み価格（10%）
tax_rate = 0.1
tax = total * tax_rate
total_with_tax = total + tax
print("税込み金額:")
print(total_with_tax)
```

---

## 📊 実践例：温度変換

```python
# 摂氏から華氏への変換
celsius = 25
fahrenheit = celsius * 9 / 5 + 32

print("摂氏:")
print(celsius)
print("華氏:")
print(fahrenheit)
```

---

## ⚠️ よくあるエラー

### エラー例 1: 文字列と数値の足し算

```python
result = "10" + 5  # ❌ エラー！
```

**解決策：**
```python
result = int("10") + 5  # ✅ 15
# または
result = "10" + str(5)  # ✅ "105"
```

### エラー例 2: ゼロ除算

```python
result = 10 / 0  # ❌ エラー！
```

**エラーメッセージ：**
```
ZeroDivisionError: division by zero
```

---

## 🎯 このセクションのまとめ

| 学んだこと | 内容 |
|------------|------|
| 算術演算子 | +, -, *, /, //, %, ** |
| 文字列操作 | 連結（+）、繰り返し（*） |
| 型変換 | int(), float(), str() |
| input() | ユーザー入力の受け取り |
| 複合代入演算子 | +=, -=, *=, /= |

---

## ➡️ 次のステップ

「演習.md」で計算と文字列操作を実践しましょう！
