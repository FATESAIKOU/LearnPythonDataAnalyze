# Section3: 棒グラフとヒストグラム - 演習問題

## 📝 基本演習

---

### 演習1: 基本の棒グラフ

以下のデータで棒グラフを作成してください。

```python
import matplotlib.pyplot as plt

# 商品別の売上
products = ["Product A", "Product B", "Product C", "Product D"]
sales = [150, 200, 120, 180]

# 棒グラフを描く
# ここにコードを書く

plt.title("Sales by Product")
plt.xlabel("Product")
plt.ylabel("Sales")
plt.show()
```

**期待する出力:**
```
  Sales
    ^
 200|        ████
 180|              ████
 150|  ████
 120|        ████
    +────────────────────→
       A    B    C    D
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 商品別の売上
products = ["Product A", "Product B", "Product C", "Product D"]
sales = [150, 200, 120, 180]

# 棒グラフを描く
plt.bar(products, sales)

plt.title("Sales by Product")
plt.xlabel("Product")
plt.ylabel("Sales")
plt.show()
```

**解説:**
- `plt.bar(x, y)` で縦棒グラフを作成
- x にカテゴリ、y に値を指定

</details>

---

### 演習2: 棒グラフの色を変える

演習1の棒グラフの色を **青** に変えてください。
また、棒の縁を黒にしてください。

```python
import matplotlib.pyplot as plt

products = ["Product A", "Product B", "Product C", "Product D"]
sales = [150, 200, 120, 180]

# 青色で、縁が黒の棒グラフを描く
# ここにコードを書く

plt.title("Sales by Product")
plt.xlabel("Product")
plt.ylabel("Sales")
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

products = ["Product A", "Product B", "Product C", "Product D"]
sales = [150, 200, 120, 180]

# 青色で、縁が黒の棒グラフを描く
plt.bar(products, sales, color='blue', edgecolor='black')

plt.title("Sales by Product")
plt.xlabel("Product")
plt.ylabel("Sales")
plt.show()
```

**解説:**
- `color='blue'`: 棒の色
- `edgecolor='black'`: 棒の縁の色

</details>

---

### 演習3: 横棒グラフ

以下のデータで **横棒グラフ** を作成してください。

```python
import matplotlib.pyplot as plt

# 各店舗の来店者数
stores = ["Shibuya", "Shinjuku", "Ikebukuro", "Tokyo", "Ueno"]
visitors = [500, 450, 380, 420, 300]

# 横棒グラフを描く
# ここにコードを書く

plt.title("Visitors by Store")
plt.xlabel("Visitors")
plt.ylabel("Store")
plt.show()
```

**期待する出力:**
```
         0   100  200  300  400  500
         +───────────────────────────→
 Shibuya |████████████████████████████|
Shinjuku |████████████████████████|
Ikebukuro|██████████████████|
   Tokyo |████████████████████|
    Ueno |██████████████|
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 各店舗の来店者数
stores = ["Shibuya", "Shinjuku", "Ikebukuro", "Tokyo", "Ueno"]
visitors = [500, 450, 380, 420, 300]

# 横棒グラフを描く
plt.barh(stores, visitors, color='coral')

plt.title("Visitors by Store")
plt.xlabel("Visitors")
plt.ylabel("Store")
plt.show()
```

**解説:**
- `plt.barh()` で横棒グラフを作成
- カテゴリ名が長い場合に便利

</details>

---

### 演習4: 基本のヒストグラム

以下のテストの点数データでヒストグラムを作成してください。

```python
import matplotlib.pyplot as plt

# 30人のテスト点数
scores = [55, 62, 68, 70, 72, 75, 77, 78, 80, 82,
          83, 85, 86, 87, 88, 89, 90, 91, 92, 93,
          94, 95, 65, 71, 76, 79, 81, 84, 88, 96]

# ヒストグラムを描く
# ここにコードを書く

plt.title("Score Distribution")
plt.xlabel("Score")
plt.ylabel("Frequency")
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 30人のテスト点数
scores = [55, 62, 68, 70, 72, 75, 77, 78, 80, 82,
          83, 85, 86, 87, 88, 89, 90, 91, 92, 93,
          94, 95, 65, 71, 76, 79, 81, 84, 88, 96]

# ヒストグラムを描く
plt.hist(scores)

plt.title("Score Distribution")
plt.xlabel("Score")
plt.ylabel("Frequency")
plt.show()
```

**解説:**
- `plt.hist(data)` でヒストグラムを作成
- デフォルトでは10個の区間（bins）に分割

</details>

---

### 演習5: ヒストグラムのビン数を変える

演習4のヒストグラムのビン数を **5** に変えてください。
また、色を `skyblue`、縁を黒にしてください。

```python
import matplotlib.pyplot as plt

scores = [55, 62, 68, 70, 72, 75, 77, 78, 80, 82,
          83, 85, 86, 87, 88, 89, 90, 91, 92, 93,
          94, 95, 65, 71, 76, 79, 81, 84, 88, 96]

# ビン数=5、色=skyblue、縁=黒のヒストグラム
# ここにコードを書く

plt.title("Score Distribution")
plt.xlabel("Score")
plt.ylabel("Frequency")
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

scores = [55, 62, 68, 70, 72, 75, 77, 78, 80, 82,
          83, 85, 86, 87, 88, 89, 90, 91, 92, 93,
          94, 95, 65, 71, 76, 79, 81, 84, 88, 96]

# ビン数=5、色=skyblue、縁=黒のヒストグラム
plt.hist(scores, bins=5, color='skyblue', edgecolor='black')

plt.title("Score Distribution")
plt.xlabel("Score")
plt.ylabel("Frequency")
plt.show()
```

**解説:**
- `bins=5`: 5つの区間に分割
- ビン数を変えると分布の見え方が変わる

</details>

---

### 演習6: 基本の円グラフ

以下のデータで円グラフを作成してください。

```python
import matplotlib.pyplot as plt

# 売上構成比
categories = ["Food", "Drink", "Snack", "Other"]
values = [45, 25, 20, 10]

# 円グラフを描く
# ここにコードを書く

plt.title("Sales Share")
plt.show()
```

**期待する出力イメージ:**
```
        .-----------.
      /       |       \
     /  Food  |  Drink \
    |   45%   |   25%   |
    |         |         |
    |         *         |
     \       / \       /
      \ Snack   Other /
       \ 20%     10% /
         '---------'
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 売上構成比
categories = ["Food", "Drink", "Snack", "Other"]
values = [45, 25, 20, 10]

# 円グラフを描く
plt.pie(values, labels=categories)

plt.title("Sales Share")
plt.show()
```

**解説:**
- `plt.pie(values, labels=labels)` で円グラフを作成
- values は各カテゴリの値（自動的に割合を計算）

</details>

---

### 演習7: 円グラフにパーセントを表示

演習6の円グラフにパーセンテージを表示してください。
また、12時の方向から開始するようにしてください。

```python
import matplotlib.pyplot as plt

categories = ["Food", "Drink", "Snack", "Other"]
values = [45, 25, 20, 10]

# パーセント表示、12時から開始
# ここにコードを書く

plt.title("Sales Share")
plt.axis('equal')  # 正円にする
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

categories = ["Food", "Drink", "Snack", "Other"]
values = [45, 25, 20, 10]

# パーセント表示、12時から開始
plt.pie(values, 
        labels=categories,
        autopct='%1.1f%%',   # パーセント表示
        startangle=90)       # 12時の方向から開始

plt.title("Sales Share")
plt.axis('equal')  # 正円にする
plt.show()
```

**解説:**
- `autopct='%1.1f%%'`: 小数点1桁でパーセント表示
- `startangle=90`: 12時の方向（90度）から開始

</details>

---

### 演習8: 箱ひげ図

以下の2クラスの点数データで箱ひげ図を作成してください。

```python
import matplotlib.pyplot as plt

# 2クラスの点数
class_a = [65, 70, 72, 75, 78, 80, 82, 85, 88, 90]
class_b = [60, 68, 75, 78, 80, 82, 85, 90, 95, 98]

# 箱ひげ図を描く
# ここにコードを書く

plt.title("Score Distribution by Class")
plt.ylabel("Score")
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 2クラスの点数
class_a = [65, 70, 72, 75, 78, 80, 82, 85, 88, 90]
class_b = [60, 68, 75, 78, 80, 82, 85, 90, 95, 98]

# データをリストにまとめる
data = [class_a, class_b]

# 箱ひげ図を描く
plt.boxplot(data, labels=['Class A', 'Class B'])

plt.title("Score Distribution by Class")
plt.ylabel("Score")
plt.grid(axis='y', alpha=0.3)
plt.show()
```

**解説:**
- `plt.boxplot(data, labels=labels)` で箱ひげ図を作成
- data はリストのリスト形式で渡す

</details>

---

### 演習9: グループ化棒グラフ

以下の2年分のデータで、グループ化棒グラフを作成してください。

```python
import matplotlib.pyplot as plt
import numpy as np

cities = ["Tokyo", "Osaka", "Nagoya"]
sales_2023 = [100, 85, 70]
sales_2024 = [120, 95, 80]

# グループ化棒グラフを描く
# ヒント: x軸の位置を計算して、棒をずらす
# ここにコードを書く

plt.title("Sales by Year")
plt.ylabel("Sales")
plt.legend()
plt.show()
```

**期待する出力イメージ:**
```
  Sales
    ^
 120|  ▓▓
 100|██▓▓
  95|      ▓▓
  85|    ██▓▓
  80|          ▓▓
  70|        ██▓▓
    +──────────────────→
       Tokyo Osaka Nagoya

  ██ = 2023   ▓▓ = 2024
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt
import numpy as np

cities = ["Tokyo", "Osaka", "Nagoya"]
sales_2023 = [100, 85, 70]
sales_2024 = [120, 95, 80]

# x軸の位置を計算
x = np.arange(len(cities))  # [0, 1, 2]
width = 0.35  # 棒の幅

# グループ化棒グラフを描く
plt.bar(x - width/2, sales_2023, width, label='2023', color='steelblue')
plt.bar(x + width/2, sales_2024, width, label='2024', color='coral')

# x軸のラベルを設定
plt.xticks(x, cities)

plt.title("Sales by Year")
plt.ylabel("Sales")
plt.legend()
plt.grid(axis='y', alpha=0.3)
plt.show()
```

**解説:**
- `np.arange(len(cities))` で位置 [0, 1, 2] を作成
- `x - width/2` と `x + width/2` で棒を左右にずらす
- `plt.xticks(x, cities)` でラベルを中央に配置

</details>

---

### 演習10: 積み上げ棒グラフ

以下のデータで積み上げ棒グラフを作成してください。

```python
import matplotlib.pyplot as plt

cities = ["Tokyo", "Osaka", "Nagoya"]
q1 = [50, 45, 35]   # 第1四半期
q2 = [60, 50, 40]   # 第2四半期

# 積み上げ棒グラフを描く
# ヒント: bottom パラメータを使う
# ここにコードを書く

plt.title("Quarterly Sales (Stacked)")
plt.ylabel("Sales")
plt.legend()
plt.show()
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

cities = ["Tokyo", "Osaka", "Nagoya"]
q1 = [50, 45, 35]   # 第1四半期
q2 = [60, 50, 40]   # 第2四半期

# 積み上げ棒グラフを描く
plt.bar(cities, q1, label='Q1', color='steelblue')
plt.bar(cities, q2, bottom=q1, label='Q2', color='coral')
# bottom=q1 で、q1の上にq2を積み上げる

plt.title("Quarterly Sales (Stacked)")
plt.ylabel("Sales")
plt.legend()
plt.grid(axis='y', alpha=0.3)
plt.show()
```

**解説:**
- `bottom=q1` で Q1 の上に Q2 を積み上げ
- 棒全体の高さが合計を表す

</details>

---

## ✅ 演習のまとめ

| 演習 | 学んだこと |
|------|------------|
| 1 | 基本の棒グラフ |
| 2 | 棒グラフの色 |
| 3 | 横棒グラフ |
| 4 | 基本のヒストグラム |
| 5 | ビン数の設定 |
| 6 | 基本の円グラフ |
| 7 | パーセント表示 |
| 8 | 箱ひげ図 |
| 9 | グループ化棒グラフ |
| 10 | 積み上げ棒グラフ |

---

## 🔗 次のステップ

これらの基本を理解したら、「応用問題」で実践的な課題に挑戦しましょう！
