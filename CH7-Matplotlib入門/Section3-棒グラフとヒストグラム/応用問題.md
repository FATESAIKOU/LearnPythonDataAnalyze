# Section3: 棒グラフとヒストグラム - 応用問題

## 🚀 実践課題

---

### 応用問題1: 月別売上比較

以下のデータを使って、**3都市の月別売上を比較** するグループ化棒グラフを作成してください。

**データ:**
```python
months = ["Jan", "Feb", "Mar"]
tokyo = [100, 120, 110]
osaka = [80, 95, 90]
nagoya = [60, 70, 75]
```

**要件:**
1. 3都市を色分けして並べる
2. Tokyo: 青、Osaka: 赤、Nagoya: 緑
3. 凡例を右上に配置
4. y軸にグリッドを表示
5. タイトル: "Monthly Sales by City"

**期待する出力イメージ:**
```
  Sales
    ^
 120|    █
 110|        █
 100|█
  95|    ▓
  90|        ▓
  80|▓
  75|        ░
  70|    ░
  60|░
    +──────────────────────→
      Jan    Feb    Mar

  █ = Tokyo  ▓ = Osaka  ░ = Nagoya
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt
import numpy as np

months = ["Jan", "Feb", "Mar"]
tokyo = [100, 120, 110]
osaka = [80, 95, 90]
nagoya = [60, 70, 75]

# x軸の位置
x = np.arange(len(months))
width = 0.25  # 棒の幅（3つ並べるので狭め）

plt.figure(figsize=(10, 6))

# 3都市を並べる
plt.bar(x - width, tokyo, width, label='Tokyo', color='blue')
plt.bar(x, osaka, width, label='Osaka', color='red')
plt.bar(x + width, nagoya, width, label='Nagoya', color='green')

# 装飾
plt.xlabel("Month", fontsize=12)
plt.ylabel("Sales", fontsize=12)
plt.title("Monthly Sales by City", fontsize=14)
plt.xticks(x, months)
plt.legend(loc='upper right')
plt.grid(axis='y', alpha=0.3)

plt.tight_layout()
plt.savefig("monthly_sales_comparison.png", dpi=150)
plt.show()
```

**解説:**
- 3系列の場合は width を狭く（0.25程度）
- 位置を `-width`, `0`, `+width` でずらす

</details>

---

### 応用問題2: 売上構成の可視化

以下のデータで、**積み上げ棒グラフと円グラフ** を並べて表示してください。

**データ:**
```python
categories = ["Food", "Drink", "Dessert", "Other"]
tokyo = [50, 30, 15, 5]
osaka = [45, 35, 12, 8]
```

**要件:**
1. 左: 積み上げ棒グラフ（Tokyo と Osaka を比較）
2. 右: 円グラフ（Tokyo のみの構成比）
3. 円グラフにはパーセントを表示
4. `plt.subplots(1, 2)` を使って横に並べる

**期待する出力イメージ:**
```
【積み上げ棒グラフ】           【円グラフ】
   ^
100|  ▓▓▓  ▓▓▓                    .----------.
 80|  ▓▓▓  ▓▓▓                  /   Food   |    \
 60|  ░░░  ░░░                 /    50%    | 30% \
 40|  ░░░  ░░░                |   Drink   |      |
 20|  ███  ███                 \          |     /
   +───────────                  \  15% | 5% /
    Tokyo Osaka                   '--------'
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt
import numpy as np

categories = ["Food", "Drink", "Dessert", "Other"]
tokyo = [50, 30, 15, 5]
osaka = [45, 35, 12, 8]
colors = ['#ff9999', '#66b3ff', '#99ff99', '#ffcc99']

# 2つのグラフを横に並べる
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 6))

# === 左: 積み上げ棒グラフ ===
cities = ['Tokyo', 'Osaka']
x = np.arange(len(cities))

# 累積値を計算して積み上げ
bottom_tokyo = [0, 0]
for i, cat in enumerate(categories):
    values = [tokyo[i], osaka[i]]
    ax1.bar(x, values, bottom=bottom_tokyo, label=cat, color=colors[i])
    # bottom を更新
    bottom_tokyo = [bottom_tokyo[0] + tokyo[i], bottom_tokyo[1] + osaka[i]]

ax1.set_ylabel('Sales', fontsize=12)
ax1.set_title('Sales Breakdown by City', fontsize=14)
ax1.set_xticks(x)
ax1.set_xticklabels(cities)
ax1.legend(loc='upper right')
ax1.grid(axis='y', alpha=0.3)

# === 右: 円グラフ（Tokyo のみ）===
ax2.pie(tokyo, 
        labels=categories,
        autopct='%1.1f%%',
        startangle=90,
        colors=colors,
        wedgeprops={'edgecolor': 'black', 'linewidth': 1})
ax2.set_title('Tokyo Sales Share', fontsize=14)

plt.tight_layout()
plt.savefig("sales_breakdown.png", dpi=150)
plt.show()
```

**別解（より簡単な積み上げ）:**

```python
import matplotlib.pyplot as plt
import numpy as np

categories = ["Food", "Drink", "Dessert", "Other"]
tokyo = [50, 30, 15, 5]
osaka = [45, 35, 12, 8]
colors = ['#ff9999', '#66b3ff', '#99ff99', '#ffcc99']

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 6))

# === 左: 積み上げ棒グラフ ===
cities = ['Tokyo', 'Osaka']

# NumPy配列に変換
tokyo_arr = np.array(tokyo)
osaka_arr = np.array(osaka)

# 各都市ごとに積み上げ
ax1.bar(cities[0], tokyo[0], color=colors[0], label='Food')
ax1.bar(cities[0], tokyo[1], bottom=tokyo[0], color=colors[1], label='Drink')
ax1.bar(cities[0], tokyo[2], bottom=sum(tokyo[:2]), color=colors[2], label='Dessert')
ax1.bar(cities[0], tokyo[3], bottom=sum(tokyo[:3]), color=colors[3], label='Other')

ax1.bar(cities[1], osaka[0], color=colors[0])
ax1.bar(cities[1], osaka[1], bottom=osaka[0], color=colors[1])
ax1.bar(cities[1], osaka[2], bottom=sum(osaka[:2]), color=colors[2])
ax1.bar(cities[1], osaka[3], bottom=sum(osaka[:3]), color=colors[3])

ax1.set_ylabel('Sales', fontsize=12)
ax1.set_title('Sales Breakdown by City', fontsize=14)
ax1.legend(loc='upper right')
ax1.grid(axis='y', alpha=0.3)

# === 右: 円グラフ ===
ax2.pie(tokyo, 
        labels=categories,
        autopct='%1.1f%%',
        startangle=90,
        colors=colors,
        wedgeprops={'edgecolor': 'black', 'linewidth': 1})
ax2.set_title('Tokyo Sales Share', fontsize=14)

plt.tight_layout()
plt.show()
```

</details>

---

### 応用問題3: 成績分布の分析

以下のテスト成績データで、**ヒストグラムと箱ひげ図** を作成してください。

**データ:**
```python
import random
random.seed(42)

# 3クラスの成績（正規分布っぽいデータ）
class_a = [random.gauss(70, 12) for _ in range(40)]
class_b = [random.gauss(75, 10) for _ in range(40)]
class_c = [random.gauss(65, 15) for _ in range(40)]
```

**要件:**
1. 上段: 3クラスのヒストグラムを重ねて表示
   - 透明度 0.5 で重ねる
   - ビン数: 15
2. 下段: 3クラスの箱ひげ図を並べて表示
3. `plt.subplots(2, 1)` で縦に並べる

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt
import random

random.seed(42)

# 3クラスの成績
class_a = [random.gauss(70, 12) for _ in range(40)]
class_b = [random.gauss(75, 10) for _ in range(40)]
class_c = [random.gauss(65, 15) for _ in range(40)]

# 縦に2つ並べる
fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(12, 10))

# === 上段: ヒストグラム ===
ax1.hist(class_a, bins=15, alpha=0.5, label='Class A', color='blue')
ax1.hist(class_b, bins=15, alpha=0.5, label='Class B', color='red')
ax1.hist(class_c, bins=15, alpha=0.5, label='Class C', color='green')

ax1.set_xlabel('Score', fontsize=12)
ax1.set_ylabel('Frequency', fontsize=12)
ax1.set_title('Score Distribution (Histogram)', fontsize=14)
ax1.legend()
ax1.grid(axis='y', alpha=0.3)

# === 下段: 箱ひげ図 ===
data = [class_a, class_b, class_c]
ax2.boxplot(data, labels=['Class A', 'Class B', 'Class C'])

ax2.set_xlabel('Class', fontsize=12)
ax2.set_ylabel('Score', fontsize=12)
ax2.set_title('Score Distribution (Box Plot)', fontsize=14)
ax2.grid(axis='y', alpha=0.3)

plt.tight_layout()
plt.savefig("score_analysis.png", dpi=150)
plt.show()
```

**解説:**
- ヒストグラム: 分布の形状を確認
- 箱ひげ図: 中央値、四分位範囲、外れ値を比較
- 2つを組み合わせると分布の特徴がよくわかる

</details>

---

### 応用問題4: 売上ダッシュボード

以下のデータを使って、**4つのグラフを2x2で配置** したダッシュボードを作成してください。

**データ:**
```python
# 月別売上
months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun"]
sales = [80, 95, 110, 105, 120, 130]

# 製品別売上
products = ["A", "B", "C", "D"]
product_sales = [40, 30, 20, 10]

# 地域別売上
regions = ["East", "West", "North", "South"]
region_sales = [35, 25, 22, 18]
```

**要件:**
1. 左上: 月別売上の折れ線グラフ
2. 右上: 製品別売上の棒グラフ
3. 左下: 地域別売上の円グラフ（パーセント表示）
4. 右下: 月別売上のヒストグラム

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# データ
months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun"]
sales = [80, 95, 110, 105, 120, 130]

products = ["A", "B", "C", "D"]
product_sales = [40, 30, 20, 10]

regions = ["East", "West", "North", "South"]
region_sales = [35, 25, 22, 18]

# 2x2 のグリッド
fig, axes = plt.subplots(2, 2, figsize=(14, 10))

# === 左上: 折れ線グラフ ===
ax1 = axes[0, 0]
ax1.plot(months, sales, 'bo-', linewidth=2, markersize=8)
ax1.set_xlabel('Month')
ax1.set_ylabel('Sales')
ax1.set_title('Monthly Sales Trend')
ax1.grid(True, alpha=0.3)

# === 右上: 棒グラフ ===
ax2 = axes[0, 1]
ax2.bar(products, product_sales, color='steelblue', edgecolor='black')
ax2.set_xlabel('Product')
ax2.set_ylabel('Sales')
ax2.set_title('Sales by Product')
ax2.grid(axis='y', alpha=0.3)

# === 左下: 円グラフ ===
ax3 = axes[1, 0]
colors = ['#ff9999', '#66b3ff', '#99ff99', '#ffcc99']
ax3.pie(region_sales, 
        labels=regions,
        autopct='%1.1f%%',
        startangle=90,
        colors=colors,
        wedgeprops={'edgecolor': 'black', 'linewidth': 1})
ax3.set_title('Sales by Region')

# === 右下: ヒストグラム ===
ax4 = axes[1, 1]
ax4.hist(sales, bins=5, color='coral', edgecolor='black', alpha=0.7)
ax4.set_xlabel('Sales')
ax4.set_ylabel('Frequency')
ax4.set_title('Sales Distribution')
ax4.grid(axis='y', alpha=0.3)

# 全体のタイトル
fig.suptitle('Sales Dashboard', fontsize=16, fontweight='bold')

plt.tight_layout()
plt.subplots_adjust(top=0.92)  # 全体タイトル用のスペース
plt.savefig("sales_dashboard.png", dpi=150)
plt.show()
```

**解説:**
- `plt.subplots(2, 2)` で2x2のグリッドを作成
- `fig.suptitle()` で全体のタイトルを設定
- `plt.subplots_adjust(top=0.92)` でタイトル用のスペースを確保

</details>

---

### 応用問題5: Pandas との連携

以下の Pandas DataFrame を使って、グラフを作成してください。

```python
import pandas as pd

# 店舗ごとの月別売上
data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
    'store_a': [100, 120, 110, 130, 140, 150],
    'store_b': [80, 95, 90, 105, 115, 120],
    'store_c': [60, 70, 75, 85, 90, 95]
}

df = pd.DataFrame(data)
```

**要件:**
1. グループ化棒グラフで3店舗を比較
2. DataFrameの列を使って描画
3. 凡例、タイトル、軸ラベルを設定

<details>
<summary>解答例を見る</summary>

**方法1: matplotlib で描画**

```python
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

# データ
data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
    'store_a': [100, 120, 110, 130, 140, 150],
    'store_b': [80, 95, 90, 105, 115, 120],
    'store_c': [60, 70, 75, 85, 90, 95]
}

df = pd.DataFrame(data)

# x軸の位置
x = np.arange(len(df['month']))
width = 0.25

plt.figure(figsize=(12, 6))

# 3店舗を並べる
plt.bar(x - width, df['store_a'], width, label='Store A', color='steelblue')
plt.bar(x, df['store_b'], width, label='Store B', color='coral')
plt.bar(x + width, df['store_c'], width, label='Store C', color='seagreen')

plt.xlabel('Month', fontsize=12)
plt.ylabel('Sales', fontsize=12)
plt.title('Monthly Sales by Store', fontsize=14)
plt.xticks(x, df['month'])
plt.legend()
plt.grid(axis='y', alpha=0.3)

plt.tight_layout()
plt.savefig("store_sales.png", dpi=150)
plt.show()
```

**方法2: Pandas の plot() メソッドを使用**

```python
import pandas as pd
import matplotlib.pyplot as plt

# データ
data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
    'store_a': [100, 120, 110, 130, 140, 150],
    'store_b': [80, 95, 90, 105, 115, 120],
    'store_c': [60, 70, 75, 85, 90, 95]
}

df = pd.DataFrame(data)

# month を index に設定
df = df.set_index('month')

# Pandas の plot() でグループ化棒グラフ
df.plot(kind='bar', figsize=(12, 6), edgecolor='black')

plt.title('Monthly Sales by Store', fontsize=14)
plt.xlabel('Month', fontsize=12)
plt.ylabel('Sales', fontsize=12)
plt.legend(title='Store')
plt.grid(axis='y', alpha=0.3)
plt.xticks(rotation=0)

plt.tight_layout()
plt.show()
```

**解説:**
- Pandas の `df.plot(kind='bar')` で簡単に棒グラフを作成
- index がx軸、各列が系列になる

</details>

---

## ✅ 応用問題のまとめ

| 問題 | テーマ | ポイント |
|------|--------|----------|
| 1 | 月別売上比較 | 3系列のグループ化棒グラフ |
| 2 | 売上構成 | 積み上げ棒グラフ + 円グラフ |
| 3 | 成績分布 | ヒストグラム + 箱ひげ図 |
| 4 | ダッシュボード | 2x2 グリッドでの配置 |
| 5 | Pandas連携 | DataFrame からグラフ作成 |

---

## 🔗 次のセクション

次は「Section4: グラフの装飾」で、
グラフをより見やすく美しくする方法を学びます！
