# Section1: Matplotlib の基本 - 応用問題

## 応用問題1: 週間気温グラフ

### 問題

1 週間の気温データをグラフにしてください。

```python
days = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"]
high_temp = [25, 27, 24, 28, 30, 29, 26]
low_temp = [18, 19, 17, 20, 22, 21, 19]
```

#### タスク1: 基本グラフ

最高気温と最低気温の両方を同じグラフに描いてください。

**期待する出力イメージ:**
```
        Weekly Temperature
      ^
   30 |            *
   29 |               *
   28 |         *
   27 |   *
   26 |                  *
   25 |*
   24 |      *
   22 |            o
   21 |               o
   20 |         o
   19 |   o               o
   18 |*
   17 |      o
      +-------------------------> Day
       Mon Tue Wed Thu Fri Sat Sun

  * = High    o = Low
```

#### タスク2: 装飾を追加

以下の装飾を追加してください：
- タイトル: "Weekly Temperature"
- x 軸ラベル: "Day"
- y 軸ラベル: "Temperature (°C)"
- 凡例: "High", "Low"
- マーカー付き

#### タスク3: 保存

グラフを「weekly_temp.png」として保存してください。

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

days = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"]
high_temp = [25, 27, 24, 28, 30, 29, 26]
low_temp = [18, 19, 17, 20, 22, 21, 19]

# Figure サイズを指定
plt.figure(figsize=(10, 6))

# 2つの線を描画
plt.plot(days, high_temp, marker='o', label="High")
plt.plot(days, low_temp, marker='o', label="Low")

# 装飾
plt.title("Weekly Temperature", fontsize=16)
plt.xlabel("Day", fontsize=12)
plt.ylabel("Temperature (°C)", fontsize=12)
plt.legend()
plt.grid(True)

# 保存
plt.savefig("weekly_temp.png", dpi=150)

plt.show()
```

</details>

---

## 応用問題2: 月別売上比較

### 問題

3 年分の月別売上データを比較するグラフを作成してください。

```python
months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", 
          "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
sales_2022 = [100, 120, 90, 110, 130, 150, 160, 140, 120, 130, 150, 180]
sales_2023 = [110, 130, 100, 120, 140, 160, 170, 150, 130, 140, 160, 190]
sales_2024 = [120, 140, 110, 130, 150, 170, 180, 160, 140, 150, 170, 200]
```

#### タスク1: 3 年分を同じグラフに

3 年分のデータをすべて同じグラフに描いてください。

#### タスク2: 見やすく装飾

以下の装飾を追加してください：
- タイトル: "Monthly Sales Comparison"
- 凡例: 年ごとのラベル
- マーカー付き
- 横長の Figure（figsize=(14, 6)）
- グリッド線

**期待する出力イメージ:**
```
             Monthly Sales Comparison
      ^
  200 |                                 △
  190 |                                 ○
  180 |         △                   △  *
  170 |            △  △               ○
  160 |   ○  ○           ○  ○      ○  *
  150 |*        *     *     *  *  *  *
  140 |○  ○     ○        ○     ○
  130 |*     *     *        *     *
  120 |△        △     △           △
  110 |      △        △     △
  100 |*
   90 |      *
      +-----------------------------------------> Month
       Jan Feb Mar Apr May Jun Jul Aug Sep Oct Nov Dec

  * = 2022    ○ = 2023    △ = 2024
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", 
          "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
sales_2022 = [100, 120, 90, 110, 130, 150, 160, 140, 120, 130, 150, 180]
sales_2023 = [110, 130, 100, 120, 140, 160, 170, 150, 130, 140, 160, 190]
sales_2024 = [120, 140, 110, 130, 150, 170, 180, 160, 140, 150, 170, 200]

# 横長の Figure
plt.figure(figsize=(14, 6))

# 3 年分を描画
plt.plot(months, sales_2022, marker='o', label="2022")
plt.plot(months, sales_2023, marker='s', label="2023")  # s = 四角
plt.plot(months, sales_2024, marker='^', label="2024")  # ^ = 三角

# 装飾
plt.title("Monthly Sales Comparison", fontsize=16)
plt.xlabel("Month", fontsize=12)
plt.ylabel("Sales", fontsize=12)
plt.legend()
plt.grid(True)

plt.savefig("sales_comparison.png", dpi=150)
plt.show()
```

</details>

---

## 応用問題3: fig, ax を使った複数グラフ

### 問題

`plt.subplots()` を使って、fig, ax 形式でグラフを作成してください。

```python
x = [1, 2, 3, 4, 5, 6]
revenue = [100, 150, 120, 200, 180, 250]
cost = [80, 100, 90, 120, 110, 140]
profit = [20, 50, 30, 80, 70, 110]
```

#### タスク1: 3 つの線を描画

収益（Revenue）、コスト（Cost）、利益（Profit）を同じグラフに描いてください。

#### タスク2: ax のメソッドで装飾

`ax.set_title()`, `ax.set_xlabel()`, `ax.set_ylabel()`, `ax.legend()`, `ax.grid()` を使って装飾してください。

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5, 6]
revenue = [100, 150, 120, 200, 180, 250]
cost = [80, 100, 90, 120, 110, 140]
profit = [20, 50, 30, 80, 70, 110]

# fig, ax を作成
fig, ax = plt.subplots(figsize=(10, 6))

# 3 つの線を描画
ax.plot(x, revenue, marker='o', label="Revenue")
ax.plot(x, cost, marker='s', label="Cost")
ax.plot(x, profit, marker='^', label="Profit")

# ax のメソッドで装飾
ax.set_title("Revenue, Cost, and Profit", fontsize=16)
ax.set_xlabel("Month", fontsize=12)
ax.set_ylabel("Amount", fontsize=12)
ax.legend()
ax.grid(True)

plt.savefig("revenue_cost_profit.png", dpi=150)
plt.show()
```

</details>

---

## 応用問題4: Pandas と組み合わせる

### 問題

Pandas DataFrame のデータを Matplotlib で可視化してください。

```python
import pandas as pd

df = pd.DataFrame({
    "Month": ["Jan", "Feb", "Mar", "Apr", "May", "Jun"],
    "Sales": [100, 150, 120, 200, 250, 300],
    "Customers": [50, 75, 60, 100, 125, 150]
})
```

#### タスク1: DataFrame からグラフ作成

Month を x 軸、Sales を y 軸にした折れ線グラフを作成してください。

#### タスク2: 2 つの y 軸

Sales と Customers を同じグラフに描いてください。
（スケールが違うため、両方見えるように工夫してください）

**期待する出力イメージ:**
```
      Sales                            Customers
        ^                                    ^
    300 |                    *         | 150
    250 |               *              | 125
    200 |         *                    | 100
    150 |   *                          |  75
    120 |      *                       |  60
    100 |*                             |  50
        +----------------------------->+
         Jan Feb Mar Apr May Jun
```

<details>
<summary>解答例を見る</summary>

**タスク1:**
```python
import matplotlib.pyplot as plt
import pandas as pd

df = pd.DataFrame({
    "Month": ["Jan", "Feb", "Mar", "Apr", "May", "Jun"],
    "Sales": [100, 150, 120, 200, 250, 300],
    "Customers": [50, 75, 60, 100, 125, 150]
})

# DataFrame から取得してプロット
plt.figure(figsize=(10, 6))
plt.plot(df["Month"], df["Sales"], marker='o')
plt.title("Monthly Sales")
plt.xlabel("Month")
plt.ylabel("Sales")
plt.grid(True)
plt.show()
```

**タスク2（発展）:**
```python
import matplotlib.pyplot as plt
import pandas as pd

df = pd.DataFrame({
    "Month": ["Jan", "Feb", "Mar", "Apr", "May", "Jun"],
    "Sales": [100, 150, 120, 200, 250, 300],
    "Customers": [50, 75, 60, 100, 125, 150]
})

# 2軸グラフ（発展的な内容）
fig, ax1 = plt.subplots(figsize=(10, 6))

# 左の y 軸: Sales
ax1.plot(df["Month"], df["Sales"], 'b-o', label="Sales")
ax1.set_xlabel("Month")
ax1.set_ylabel("Sales", color='blue')
ax1.tick_params(axis='y', labelcolor='blue')

# 右の y 軸: Customers
ax2 = ax1.twinx()  # 2つ目の y 軸を作成
ax2.plot(df["Month"], df["Customers"], 'r-s', label="Customers")
ax2.set_ylabel("Customers", color='red')
ax2.tick_params(axis='y', labelcolor='red')

plt.title("Sales and Customers")
fig.legend(loc="upper left", bbox_to_anchor=(0.15, 0.85))
plt.grid(True)
plt.show()
```

</details>

---

## 🎉 Section1 完了！

Matplotlib の基本をマスターしました！

**学んだこと:**
- `plt.plot()` で折れ線グラフを描く
- タイトル、ラベル、凡例を追加
- 複数の線を重ねて描く
- グラフを画像として保存
- `fig, ax` を使った書き方

**次のステップ:**
Section2「折れ線グラフと散布図」でより詳細なカスタマイズを学びましょう！
