# Section1: Matplotlib の基本 - 演習

## 演習1: 最初のグラフ

### 問題 1-1

以下のデータを使って、折れ線グラフを描いてください。

```python
x = [1, 2, 3, 4, 5]
y = [10, 25, 15, 35, 20]
```

**期待する出力:**
```
      y
      ^
   35 |         *
   25 |   *
   20 |               *
   15 |      *
   10 |*
      +-------------------> x
        1   2   3   4   5
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 25, 15, 35, 20]

plt.plot(x, y)
plt.show()
```

</details>

---

### 問題 1-2

y データだけを渡して、折れ線グラフを描いてください。
x 軸が自動的に 0, 1, 2, ... になることを確認してください。

```python
y = [100, 80, 120, 90, 110]
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

y = [100, 80, 120, 90, 110]

plt.plot(y)
plt.show()

# x 軸は自動的に 0, 1, 2, 3, 4 になる
```

</details>

---

## 演習2: タイトルとラベル

### 問題 2-1

以下のデータで折れ線グラフを描き、タイトル「Temperature」、x 軸ラベル「Day」、y 軸ラベル「Celsius」を追加してください。

```python
days = [1, 2, 3, 4, 5, 6, 7]
temp = [20, 22, 19, 25, 28, 26, 23]
```

**期待する出力:**
```
              Temperature
      ^
   28 |            *
   26 |               *
   25 |         *
   23 |                  *
   22 |   *
   20 |*
   19 |      *
      +-------------------------> Day
        1  2  3  4  5  6  7
              Celsius
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

days = [1, 2, 3, 4, 5, 6, 7]
temp = [20, 22, 19, 25, 28, 26, 23]

plt.plot(days, temp)
plt.title("Temperature")
plt.xlabel("Day")
plt.ylabel("Celsius")
plt.show()
```

</details>

---

### 問題 2-2

問題 2-1 のグラフに、マーカー（点）を追加してください。
ヒント: `plt.plot(x, y, marker='o')`

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

days = [1, 2, 3, 4, 5, 6, 7]
temp = [20, 22, 19, 25, 28, 26, 23]

plt.plot(days, temp, marker='o')  # marker='o' で丸い点を追加
plt.title("Temperature")
plt.xlabel("Day")
plt.ylabel("Celsius")
plt.show()
```

</details>

---

## 演習3: 複数の線

### 問題 3-1

以下の 2 つのデータを同じグラフに描いてください。

```python
x = [1, 2, 3, 4, 5]
y1 = [10, 20, 30, 40, 50]
y2 = [15, 25, 20, 35, 45]
```

**期待する出力:**
```
      y
      ^
   50 |               *
   45 |               o
   40 |            *
   35 |            o
   30 |         *
   25 |      o
   20 |   *     o
   15 |   o
   10 |*
      +-------------------> x
        1   2   3   4   5

  * = y1 の線
  o = y2 の線
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y1 = [10, 20, 30, 40, 50]
y2 = [15, 25, 20, 35, 45]

plt.plot(x, y1)
plt.plot(x, y2)
plt.show()
```

</details>

---

### 問題 3-2

問題 3-1 のグラフに凡例（legend）を追加してください。
y1 を「Product A」、y2 を「Product B」としてください。

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y1 = [10, 20, 30, 40, 50]
y2 = [15, 25, 20, 35, 45]

plt.plot(x, y1, label="Product A")
plt.plot(x, y2, label="Product B")
plt.legend()  # 凡例を表示
plt.show()
```

</details>

---

## 演習4: Figure のサイズ

### 問題 4-1

以下のデータで、横幅 12、縦幅 4 の大きなグラフを作成してください。
ヒント: `plt.figure(figsize=(横, 縦))`

```python
x = list(range(1, 13))  # 1〜12
y = [100, 120, 90, 150, 180, 200, 210, 190, 170, 160, 140, 130]
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = list(range(1, 13))
y = [100, 120, 90, 150, 180, 200, 210, 190, 170, 160, 140, 130]

plt.figure(figsize=(12, 4))  # 横長のグラフ
plt.plot(x, y, marker='o')
plt.title("Monthly Data")
plt.xlabel("Month")
plt.ylabel("Value")
plt.show()
```

</details>

---

## 演習5: グラフの保存

### 問題 5-1

以下のデータでグラフを作成し、「my_first_graph.png」として保存してください。

```python
x = [1, 2, 3, 4, 5]
y = [10, 40, 20, 50, 30]
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 40, 20, 50, 30]

plt.plot(x, y)
plt.title("My First Graph")

# 保存
plt.savefig("my_first_graph.png")

plt.show()
```

</details>

---

### 問題 5-2

問題 5-1 のグラフを、高解像度（dpi=300）で保存してください。

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 40, 20, 50, 30]

plt.plot(x, y)
plt.title("My First Graph")

# 高解像度で保存
plt.savefig("my_first_graph_hd.png", dpi=300)

plt.show()
```

</details>

---

## 演習6: fig, ax を使う

### 問題 6-1

`plt.subplots()` を使って Figure と Axes を作成し、折れ線グラフを描いてください。

```python
x = [1, 2, 3, 4, 5]
y = [10, 25, 15, 35, 20]
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 25, 15, 35, 20]

# fig, ax を作成
fig, ax = plt.subplots()

# ax にプロット
ax.plot(x, y)

# ax でタイトルを設定
ax.set_title("Using Axes")
ax.set_xlabel("X")
ax.set_ylabel("Y")

plt.show()
```

</details>

---

### 問題 6-2

`fig, ax = plt.subplots(figsize=(10, 5))` で Figure サイズを指定してグラフを描いてください。

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 25, 15, 35, 20]

# figsize を指定して作成
fig, ax = plt.subplots(figsize=(10, 5))

ax.plot(x, y, marker='o')
ax.set_title("Larger Figure")
ax.set_xlabel("X")
ax.set_ylabel("Y")
ax.grid(True)  # グリッド線を追加

plt.show()
```

</details>

---

## 🎉 演習完了！

すべての問題が解けたら、「応用問題」に進みましょう！
