# Section4: グラフの装飾 - 応用問題

## 🚀 実践課題

---

### 応用問題1: ビジネスレポート用グラフ

以下のデータを使って、**ビジネスプレゼンテーション品質** のグラフを作成してください。

**データ:**
```python
months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun']
sales = [80, 95, 110, 125, 140, 155]
target = [100, 100, 110, 120, 130, 140]
```

**要件:**
1. 売上: 青の実線・丸マーカー
2. 目標: 赤の破線
3. タイトル: 太字、フォントサイズ16、上にパディング
4. 軸ラベル: フォントサイズ12
5. 凡例: 右下、枠あり、影あり
6. y軸グリッド: 破線、透明度0.5
7. y軸の範囲: 60〜170
8. 上と右の枠線を非表示
9. 高解像度(300dpi)で保存

**期待する出力イメージ:**
```
┌───────────────────────────────────────────┐
│      Monthly Sales vs Target              │
│                                           │
│  160 |                        ●           │
│  140 |                   ●   - -          │
│  120 |              ●   - -               │
│  110 |         ●   - -                    │
│   95 |    ●   - -                         │
│   80 |●   - -                             │
│      +─────────────────────────────       │
│       Jan Feb Mar Apr May Jun             │
│                                           │
│               ●── Sales  --- Target       │
└───────────────────────────────────────────┘
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun']
sales = [80, 95, 110, 125, 140, 155]
target = [100, 100, 110, 120, 130, 140]

# Figure と Axes を作成
fig, ax = plt.subplots(figsize=(12, 7))

# データをプロット
ax.plot(months, sales, 'o-', color='#3498db', linewidth=2.5, 
        markersize=10, label='Sales', markeredgecolor='white', markeredgewidth=2)
ax.plot(months, target, '--', color='#e74c3c', linewidth=2, label='Target')

# タイトル
ax.set_title('Monthly Sales vs Target', 
             fontsize=16, fontweight='bold', pad=20)

# 軸ラベル
ax.set_xlabel('Month', fontsize=12, labelpad=10)
ax.set_ylabel('Sales (万円)', fontsize=12, labelpad=10)

# 軸の範囲
ax.set_ylim(60, 170)

# グリッド
ax.grid(True, axis='y', linestyle='--', alpha=0.5)

# 凡例
ax.legend(loc='lower right', fontsize=11, 
          frameon=True, shadow=True, facecolor='white')

# 目盛りのフォントサイズ
ax.tick_params(axis='both', labelsize=11)

# 上と右の枠線を非表示
ax.spines['top'].set_visible(False)
ax.spines['right'].set_visible(False)

# 余白を調整
plt.tight_layout()

# 高解像度で保存
plt.savefig('business_report.png', dpi=300, 
            bbox_inches='tight', facecolor='white')
plt.show()
```

**解説:**
- `ax.spines['top'].set_visible(False)` で枠線を非表示
- `markeredgecolor='white'` でマーカーに白い縁をつけてモダンな印象に

</details>

---

### 応用問題2: 複数の注釈を追加

以下のデータで、**重要なポイントに注釈を追加** したグラフを作成してください。

**データ:**
```python
months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug']
sales = [80, 75, 90, 120, 135, 125, 140, 155]
```

**要件:**
1. 3月（Mar）に「新製品発売」の注釈（矢印付き）
2. 5月（May）に「ピーク到達」の注釈（矢印付き）
3. 最終月に現在の売上値を表示

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug']
sales = [80, 75, 90, 120, 135, 125, 140, 155]

fig, ax = plt.subplots(figsize=(12, 7))

# データをプロット
ax.plot(months, sales, 'o-', color='#2ecc71', linewidth=2.5, markersize=10)

# 注釈1: 3月に新製品発売
ax.annotate('New Product\nLaunch',
            xy=(2, 90),           # 矢印の先（Mar = index 2）
            xytext=(0.5, 105),    # テキストの位置
            fontsize=11,
            arrowprops=dict(arrowstyle='->', color='#3498db', lw=2),
            bbox=dict(boxstyle='round', facecolor='#e8f4f8', edgecolor='#3498db'))

# 注釈2: 5月にピーク
ax.annotate('Peak\nReached',
            xy=(4, 135),          # 矢印の先（May = index 4）
            xytext=(5.5, 145),    # テキストの位置
            fontsize=11,
            arrowprops=dict(arrowstyle='->', color='#e74c3c', lw=2),
            bbox=dict(boxstyle='round', facecolor='#fdeaea', edgecolor='#e74c3c'))

# 最終月の値を表示
ax.text(7, 155 + 5, f'{sales[-1]}万円', 
        fontsize=12, ha='center', fontweight='bold', color='#27ae60')

# 装飾
ax.set_title('Monthly Sales Trend with Annotations', 
             fontsize=16, fontweight='bold', pad=20)
ax.set_xlabel('Month', fontsize=12)
ax.set_ylabel('Sales (万円)', fontsize=12)
ax.set_ylim(60, 180)
ax.grid(True, axis='y', linestyle='--', alpha=0.5)

# 上と右の枠線を非表示
ax.spines['top'].set_visible(False)
ax.spines['right'].set_visible(False)

plt.tight_layout()
plt.savefig('annotated_chart.png', dpi=300, bbox_inches='tight')
plt.show()
```

**解説:**
- `bbox=dict(...)` で注釈の背景に枠をつける
- 複数の注釈を配置する際は重ならないよう位置を調整

</details>

---

### 応用問題3: カスタム目盛りとグリッド

以下のデータで、**カスタム目盛りと主副グリッド** を設定したグラフを作成してください。

**データ:**
```python
x = list(range(0, 101, 10))  # [0, 10, 20, ..., 100]
y = [0, 5, 15, 30, 50, 65, 75, 82, 88, 93, 100]
```

**要件:**
1. x軸: 0, 20, 40, 60, 80, 100 のみ表示
2. y軸: 0〜100、20刻みで表示
3. 主グリッド: 実線、透明度0.7
4. 副グリッド: 点線、透明度0.4
5. 副目盛りを有効化

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = list(range(0, 101, 10))
y = [0, 5, 15, 30, 50, 65, 75, 82, 88, 93, 100]

fig, ax = plt.subplots(figsize=(10, 6))

# データをプロット
ax.plot(x, y, 'o-', color='#9b59b6', linewidth=2, markersize=8)

# x軸の目盛り
ax.set_xticks([0, 20, 40, 60, 80, 100])

# y軸の目盛り
ax.set_yticks([0, 20, 40, 60, 80, 100])

# 副目盛りを有効化
ax.minorticks_on()

# 主グリッド
ax.grid(True, which='major', linestyle='-', linewidth=0.8, alpha=0.7)

# 副グリッド
ax.grid(True, which='minor', linestyle=':', linewidth=0.5, alpha=0.4)

# 装飾
ax.set_title('Progress Curve with Custom Grid', fontsize=14, fontweight='bold')
ax.set_xlabel('Time (%)', fontsize=12)
ax.set_ylabel('Completion (%)', fontsize=12)
ax.set_xlim(0, 100)
ax.set_ylim(0, 100)

plt.tight_layout()
plt.savefig('custom_grid.png', dpi=300, bbox_inches='tight')
plt.show()
```

**解説:**
- `ax.minorticks_on()` で副目盛りを有効化
- `which='major'` と `which='minor'` でグリッドを分けて設定

</details>

---

### 応用問題4: スタイルの比較

同じデータを **3つの異なるスタイル** で描画し、横に並べてください。

**データ:**
```python
x = [1, 2, 3, 4, 5]
y = [10, 25, 20, 35, 30]
```

**要件:**
1. 左: デフォルトスタイル
2. 中央: 'ggplot' スタイル
3. 右: 'seaborn-v0_8-darkgrid' スタイル（またはお使いの環境で利用可能なスタイル）

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5]
y = [10, 25, 20, 35, 30]

# 利用可能なスタイルを確認（必要に応じて）
# print(plt.style.available)

fig, axes = plt.subplots(1, 3, figsize=(15, 5))

# 左: デフォルトスタイル
with plt.style.context('default'):
    axes[0].plot(x, y, 'o-', linewidth=2, markersize=8)
    axes[0].set_title('Default Style', fontsize=14)
    axes[0].set_xlabel('X')
    axes[0].set_ylabel('Y')
    axes[0].grid(True, alpha=0.3)

# 中央: ggplot スタイル
with plt.style.context('ggplot'):
    axes[1].plot(x, y, 'o-', linewidth=2, markersize=8)
    axes[1].set_title('ggplot Style', fontsize=14)
    axes[1].set_xlabel('X')
    axes[1].set_ylabel('Y')

# 右: seaborn スタイル（環境によって名前が異なる場合がある）
try:
    style = 'seaborn-v0_8-darkgrid'
    with plt.style.context(style):
        axes[2].plot(x, y, 'o-', linewidth=2, markersize=8)
        axes[2].set_title('Seaborn Style', fontsize=14)
        axes[2].set_xlabel('X')
        axes[2].set_ylabel('Y')
except:
    # フォールバック
    with plt.style.context('bmh'):
        axes[2].plot(x, y, 'o-', linewidth=2, markersize=8)
        axes[2].set_title('BMH Style', fontsize=14)
        axes[2].set_xlabel('X')
        axes[2].set_ylabel('Y')

plt.tight_layout()
plt.savefig('style_comparison.png', dpi=300, bbox_inches='tight')
plt.show()
```

**解説:**
- `with plt.style.context(...)` で一時的にスタイルを適用
- スタイル名は環境によって異なることがある
- `plt.style.available` で利用可能なスタイルを確認

</details>

---

### 応用問題5: 総合：プロフェッショナルなダッシュボード

以下のデータを使って、**プロフェッショナルなダッシュボード** を作成してください。

**データ:**
```python
# 月別売上
months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun']
sales = [100, 120, 115, 140, 160, 175]
target = [110, 115, 120, 130, 145, 160]

# 製品別売上
products = ['A', 'B', 'C', 'D']
product_sales = [35, 28, 22, 15]
```

**要件:**
1. 2x1のレイアウト（縦に2つ）
2. 上段: 売上と目標の折れ線グラフ
   - 売上が目標を超えた月に「目標達成!」の注釈
   - 凡例は左上、枠あり
3. 下段: 製品別売上の横棒グラフ
   - 値を棒の右側に表示
   - グラデーションカラー
4. 全体タイトル: "Sales Dashboard - Q2 2024"
5. 高解像度で保存

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt
import numpy as np

# データ
months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun']
sales = [100, 120, 115, 140, 160, 175]
target = [110, 115, 120, 130, 145, 160]

products = ['A', 'B', 'C', 'D']
product_sales = [35, 28, 22, 15]

# 図を作成
fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(12, 10))

# === 上段: 売上と目標の折れ線グラフ ===
ax1.plot(months, sales, 'o-', color='#27ae60', linewidth=2.5, 
         markersize=10, label='Sales', markeredgecolor='white', markeredgewidth=2)
ax1.plot(months, target, 's--', color='#e74c3c', linewidth=2, 
         markersize=8, label='Target')

# 売上が目標を超えた月を見つける
for i, (s, t) in enumerate(zip(sales, target)):
    if s > t:
        ax1.annotate('Target\nAchieved!',
                     xy=(i, s),
                     xytext=(i + 0.3, s + 10),
                     fontsize=9,
                     color='#27ae60',
                     fontweight='bold',
                     arrowprops=dict(arrowstyle='->', color='#27ae60', lw=1.5))
        break  # 最初の達成月のみ注釈

# 装飾
ax1.set_title('Monthly Sales vs Target', fontsize=14, fontweight='bold')
ax1.set_xlabel('Month', fontsize=11)
ax1.set_ylabel('Sales (万円)', fontsize=11)
ax1.set_ylim(80, 200)
ax1.legend(loc='upper left', fontsize=10, frameon=True, facecolor='white')
ax1.grid(True, axis='y', linestyle='--', alpha=0.5)
ax1.spines['top'].set_visible(False)
ax1.spines['right'].set_visible(False)

# === 下段: 製品別売上の横棒グラフ ===
colors = ['#3498db', '#2980b9', '#1f618d', '#154360']
bars = ax2.barh(products, product_sales, color=colors, edgecolor='white', height=0.6)

# 値を棒の右側に表示
for bar, val in zip(bars, product_sales):
    ax2.text(val + 1, bar.get_y() + bar.get_height()/2, 
             f'{val}%', va='center', fontsize=11, fontweight='bold')

# 装飾
ax2.set_title('Sales by Product', fontsize=14, fontweight='bold')
ax2.set_xlabel('Share (%)', fontsize=11)
ax2.set_xlim(0, 45)
ax2.spines['top'].set_visible(False)
ax2.spines['right'].set_visible(False)
ax2.grid(True, axis='x', linestyle='--', alpha=0.5)

# 全体のタイトル
fig.suptitle('Sales Dashboard - Q2 2024', 
             fontsize=18, fontweight='bold', y=0.98)

# 余白を調整
plt.tight_layout()
plt.subplots_adjust(top=0.92)

# 高解像度で保存
plt.savefig('professional_dashboard.png', dpi=300, 
            bbox_inches='tight', facecolor='white')
plt.show()
```

**解説:**
- 棒の右側に値を表示するには `ax.text()` を使用
- グラデーションカラーはリストで指定
- `fig.suptitle()` で全体タイトルを追加
- `plt.subplots_adjust(top=0.92)` で全体タイトル用のスペースを確保

</details>

---

## ✅ 応用問題のまとめ

| 問題 | テーマ | ポイント |
|------|--------|----------|
| 1 | ビジネスレポート | 総合的な装飾 |
| 2 | 複数の注釈 | annotate の応用 |
| 3 | カスタムグリッド | 主/副グリッド |
| 4 | スタイル比較 | plt.style の活用 |
| 5 | ダッシュボード | 総合演習 |

---

## 🔗 次のセクション

次は「Section5: 複数グラフとエクスポート」で、
複数のグラフを効率的に配置し、様々な形式で保存する方法を学びます！
