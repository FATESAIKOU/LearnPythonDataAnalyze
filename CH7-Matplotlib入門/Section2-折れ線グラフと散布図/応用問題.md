# Section2: 折れ線グラフと散布図 - 応用問題

## 🚀 実践課題

---

### 応用問題1: 株価チャート

以下の株価データを使って、**視覚的に見やすい株価チャート** を作成してください。

**データ:**
```python
days = ["Mon", "Tue", "Wed", "Thu", "Fri"]
stock_a = [1000, 1050, 980, 1100, 1080]
stock_b = [500, 520, 480, 550, 530]
```

**要件:**
1. 2つの株価を1つのグラフに表示
2. Stock A: 赤・実線・丸マーカー
3. Stock B: 青・破線・四角マーカー
4. 線の太さ: 2、マーカーサイズ: 8
5. 凡例、タイトル、軸ラベルを設定
6. グリッドを表示

**期待する出力イメージ:**
```
        Stock Price Comparison
    ^
1100|              ●
1080|                  ●
1050|    ●
1000|●
 980|        ●
    |
 550|              ■
 530|                  ■
 520|    ■
 500|■
 480|        ■
    +──────────────────────→
     Mon Tue Wed Thu Fri

    ●─── Stock A   ■--- Stock B
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

days = ["Mon", "Tue", "Wed", "Thu", "Fri"]
stock_a = [1000, 1050, 980, 1100, 1080]
stock_b = [500, 520, 480, 550, 530]

plt.figure(figsize=(10, 6))

# 2つの株価を描画
plt.plot(days, stock_a, 'ro-', 
         label='Stock A', linewidth=2, markersize=8)
plt.plot(days, stock_b, 'bs--', 
         label='Stock B', linewidth=2, markersize=8)

# 装飾
plt.title("Stock Price Comparison", fontsize=14)
plt.xlabel("Day", fontsize=12)
plt.ylabel("Price (Yen)", fontsize=12)
plt.legend(loc='upper left')
plt.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig("stock_chart.png", dpi=150)
plt.show()
```

**解説:**
- `'ro-'`: 赤・丸・実線
- `'bs--'`: 青・四角・破線
- `loc='upper left'`: 凡例の位置指定

</details>

---

### 応用問題2: 相関分析散布図

**身長と体重の関係** を可視化する散布図を作成してください。

**データ:**
```python
# 10人のデータ
height = [155, 160, 162, 168, 170, 172, 175, 178, 180, 185]  # cm
weight = [50, 52, 55, 60, 63, 65, 70, 72, 75, 80]            # kg
```

**要件:**
1. 散布図で表示
2. マーカーサイズ: 100
3. 色: 緑
4. 透明度: 0.7
5. 縁の色: 黒
6. タイトル、軸ラベル、グリッドを設定

**期待する出力イメージ:**
```
  Weight (kg)
    ^
  80|                        ●
  75|                    ●
  72|                ●
  70|            ●
  65|        ●
  63|      ●
  60|    ●
  55|  ●
  52|●
  50|●
    +────────────────────────────→ Height (cm)
     155  160  165  170  175  180  185
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 10人のデータ
height = [155, 160, 162, 168, 170, 172, 175, 178, 180, 185]
weight = [50, 52, 55, 60, 63, 65, 70, 72, 75, 80]

plt.figure(figsize=(10, 6))

# 散布図を描画
plt.scatter(height, weight,
            s=100,             # サイズ
            c='green',         # 色
            alpha=0.7,         # 透明度
            edgecolor='black') # 縁の色

# 装飾
plt.title("Height vs Weight", fontsize=14)
plt.xlabel("Height (cm)", fontsize=12)
plt.ylabel("Weight (kg)", fontsize=12)
plt.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig("height_weight.png", dpi=150)
plt.show()
```

**解説:**
- 正の相関（右上がり）が確認できる
- 身長が高いほど体重も重い傾向

</details>

---

### 応用問題3: 年度別売上トレンド

3年分の月別売上データを **見やすく比較** できるグラフを作成してください。

**データ:**
```python
months = ["Apr", "May", "Jun", "Jul", "Aug", "Sep"]
sales_2022 = [80, 90, 85, 100, 110, 95]
sales_2023 = [90, 100, 95, 115, 125, 110]
sales_2024 = [100, 120, 110, 130, 145, 125]
```

**要件:**
1. 3つの線を明確に区別
2. 2022年: 緑・点線・三角
3. 2023年: 青・破線・四角
4. 2024年: 赤・実線・丸
5. 凡例を右下に配置
6. y軸の範囲を 70〜160 に設定

**期待する出力イメージ:**
```
          Monthly Sales Trend
    ^
 145|                ●
 130|          ●
 125|    ●              ■     ●
 120|                ■
 115|          ■
 110|        ●    △        ●
 100|●   ■                △
  95|      ●    ■            △
  90|△   ●
  85|      △
  80|△
    +──────────────────────────→
     Apr May Jun Jul Aug Sep

      △···· 2022  ■---- 2023  ●──── 2024
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

months = ["Apr", "May", "Jun", "Jul", "Aug", "Sep"]
sales_2022 = [80, 90, 85, 100, 110, 95]
sales_2023 = [90, 100, 95, 115, 125, 110]
sales_2024 = [100, 120, 110, 130, 145, 125]

plt.figure(figsize=(12, 6))

# 3つの年度を描画
plt.plot(months, sales_2022, 'g:^', 
         label='2022', linewidth=2, markersize=8)
plt.plot(months, sales_2023, 'b--s', 
         label='2023', linewidth=2, markersize=8)
plt.plot(months, sales_2024, 'r-o', 
         label='2024', linewidth=2, markersize=8)

# 装飾
plt.title("Monthly Sales Trend", fontsize=14)
plt.xlabel("Month", fontsize=12)
plt.ylabel("Sales (万円)", fontsize=12)
plt.ylim(70, 160)  # y軸の範囲を設定
plt.legend(loc='lower right')
plt.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig("sales_trend.png", dpi=150)
plt.show()
```

**解説:**
- `'g:^'`: 緑・点線・三角
- `'b--s'`: 青・破線・四角
- `'r-o'`: 赤・実線・丸
- `plt.ylim(70, 160)`: y軸の範囲を指定

</details>

---

### 応用問題4: バブルチャート

**広告費・来店者数・売上** の3つの変数を1つの散布図で可視化してください。

**データ:**
```python
# 店舗ごとのデータ
stores = ["A", "B", "C", "D", "E", "F"]
ad_cost = [10, 25, 15, 40, 30, 50]       # 広告費（万円）
visitors = [100, 200, 150, 350, 280, 400] # 来店者数
sales = [30, 60, 45, 100, 80, 120]        # 売上（万円）
```

**要件:**
1. x軸: 広告費、y軸: 来店者数
2. 点のサイズ: 売上に比例（売上 × 5 程度）
3. 点の色: 売上に応じてグラデーション（カラーマップ使用）
4. カラーバーを追加
5. 透明度: 0.6
6. 縁の色: 黒

**期待する出力イメージ:**
```
  Visitors
    ^
 400|                     ◉ ← 売上120万
 350|              ◉ ← 売上100万
 280|         ◉ ← 売上80万
 200|    ● ← 売上60万
 150|  ○ ← 売上45万
 100|● ← 売上30万
    +──────────────────────────→ Ad Cost
     10   20   30   40   50

  ●=小     ◉=大（サイズが売上を表す）
  [カラーバー: 30 ─────→ 120]
```

<details>
<summary>解答例を見る</summary>

```python
import matplotlib.pyplot as plt

# 店舗ごとのデータ
stores = ["A", "B", "C", "D", "E", "F"]
ad_cost = [10, 25, 15, 40, 30, 50]        # 広告費
visitors = [100, 200, 150, 350, 280, 400]  # 来店者数
sales = [30, 60, 45, 100, 80, 120]         # 売上

# サイズを売上に比例させる
sizes = [s * 5 for s in sales]

plt.figure(figsize=(10, 6))

# バブルチャート
scatter = plt.scatter(ad_cost, visitors,
                      s=sizes,           # サイズ = 売上
                      c=sales,           # 色 = 売上
                      cmap='viridis',    # カラーマップ
                      alpha=0.6,         # 透明度
                      edgecolor='black') # 縁の色

# カラーバーを追加
cbar = plt.colorbar(scatter)
cbar.set_label('Sales (万円)', fontsize=12)

# 装飾
plt.title("Ad Cost vs Visitors (Size/Color = Sales)", fontsize=14)
plt.xlabel("Ad Cost (万円)", fontsize=12)
plt.ylabel("Visitors", fontsize=12)
plt.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig("bubble_chart.png", dpi=150)
plt.show()
```

**解説:**
- `s=sizes`: 各点のサイズを売上に比例
- `c=sales`: 各点の色を売上に対応
- `cmap='viridis'`: 色のグラデーション
- `plt.colorbar()`: カラーバーを表示

</details>

---

### 応用問題5: Pandasとの連携

以下のPandas DataFrameを使って、**月別の気温推移** を可視化してください。

```python
import pandas as pd
import matplotlib.pyplot as plt

# データフレームを作成
data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 
              'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
    'tokyo': [5.2, 5.7, 8.7, 13.9, 18.2, 21.4, 
              25.0, 26.4, 22.8, 17.5, 12.1, 7.6],
    'osaka': [6.0, 6.3, 9.4, 15.1, 19.7, 23.5, 
              27.4, 28.8, 25.0, 19.0, 13.6, 8.6]
}

df = pd.DataFrame(data)
```

**要件:**
1. DataFrame から直接プロット
2. Tokyo: 赤線、Osaka: 青線
3. マーカー付き（丸）
4. 凡例を表示
5. タイトル: "Monthly Temperature in Japan"
6. y軸ラベル: "Temperature (°C)"

<details>
<summary>解答例を見る</summary>

```python
import pandas as pd
import matplotlib.pyplot as plt

# データフレームを作成
data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 
              'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
    'tokyo': [5.2, 5.7, 8.7, 13.9, 18.2, 21.4, 
              25.0, 26.4, 22.8, 17.5, 12.1, 7.6],
    'osaka': [6.0, 6.3, 9.4, 15.1, 19.7, 23.5, 
              27.4, 28.8, 25.0, 19.0, 13.6, 8.6]
}

df = pd.DataFrame(data)

plt.figure(figsize=(12, 6))

# DataFrameから直接プロット
plt.plot(df['month'], df['tokyo'], 'ro-', 
         label='Tokyo', linewidth=2, markersize=6)
plt.plot(df['month'], df['osaka'], 'bs-', 
         label='Osaka', linewidth=2, markersize=6)

# 装飾
plt.title("Monthly Temperature in Japan", fontsize=14)
plt.xlabel("Month", fontsize=12)
plt.ylabel("Temperature (°C)", fontsize=12)
plt.legend()
plt.grid(True, alpha=0.3)

# x軸のラベルを回転
plt.xticks(rotation=45)

plt.tight_layout()
plt.savefig("japan_temperature.png", dpi=150)
plt.show()
```

**別解: Pandasの plot() メソッドを使う方法:**

```python
import pandas as pd
import matplotlib.pyplot as plt

data = {
    'month': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 
              'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
    'tokyo': [5.2, 5.7, 8.7, 13.9, 18.2, 21.4, 
              25.0, 26.4, 22.8, 17.5, 12.1, 7.6],
    'osaka': [6.0, 6.3, 9.4, 15.1, 19.7, 23.5, 
              27.4, 28.8, 25.0, 19.0, 13.6, 8.6]
}

df = pd.DataFrame(data)
df = df.set_index('month')

# Pandas の plot() メソッドを使用
df.plot(kind='line', marker='o', figsize=(12, 6))

plt.title("Monthly Temperature in Japan", fontsize=14)
plt.ylabel("Temperature (°C)", fontsize=12)
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()
```

**解説:**
- Pandas の列をそのまま `plt.plot()` に渡せる
- Pandas の `df.plot()` メソッドも便利

</details>

---

## ✅ 応用問題のまとめ

| 問題 | テーマ | ポイント |
|------|--------|----------|
| 1 | 株価チャート | 複数線の区別 |
| 2 | 相関分析 | 散布図の基本 |
| 3 | 売上トレンド | 3線の比較、ylim |
| 4 | バブルチャート | サイズ・色の変更 |
| 5 | Pandas連携 | DataFrame からプロット |

---

## 🔗 次のセクション

次は「Section3: 棒グラフとヒストグラム」で、
カテゴリデータと分布の可視化を学びます！
